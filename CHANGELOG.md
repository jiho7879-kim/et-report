## (미배포)

### plot 규격·타깃·축 범위 손질 (2026-08-16)
- **xy쌍마다 규격 창과 타깃을 하나씩** — x·y를 컴마로 여러 개 적어 겹쳐 그릴 때
  예전에는 전 쌍의 규격을 합집합(min low, max high)으로 뭉쳐 창 하나만 그려서,
  쌍끼리 규격이 다르면 어느 쪽에도 맞지 않는 창이 나왔다. 타깃도 평균을 내
  실제 타깃이 아닌 곳에 X가 찍혔다. 이제 쌍마다 따로 그린다 — 색은 쌍을
  구분하지 않고 규격 빨강·타깃 파랑 그대로이고, 규격이 같은 쌍은 겹쳐 그리지
  않는다.
- **타깃 X 색을 `#0000FF`로.**
- **로그 축의 여백을 decade로 준다** — 선형 폭의 10%는 decade가 여럿인 축에서
  0.04 decade밖에 안 돼 여백이 사라졌고, 아래쪽 여백은 음수가 되어 잘리는 바람에
  최솟값 점이 축선에 붙어 반쯤 그려졌다(누설 plot이 늘 그랬다). 하한을 `1e-4`로
  못박던 보정도 없앴다 — 데이터가 1e-3~1e-2면 축도 그만큼만 열린다. 규격 하한이
  0이거나 값에 음수가 섞여도(로그에 못 그리는 값) 축이 뒤집히지 않는다.
- **값이 하나뿐일 때의 여백을 값 크기에 비례하게** — ±0.5 고정이라 1e-9짜리
  누설은 축이 -0.6~0.6이 되어 점이 0에 눌러붙었고, 큰 값은 여백이 없는 것이나
  마찬가지였다. 0 근처에서만 절대 여백(±0.5)을 쓴다.
- 사용 설명서 [탐색] 절에 규격 창·타깃 읽는 법과 로그 축 여백을 넣고 PDF·캡처를
  다시 만들었다.
- 새 테스트 `tests/test_render_spec_pairs.py`(5건)·축 범위 회귀 5건.

### 데모 강화 — 모든 기능을 눌러 볼 수 있는 한 벌 (2026-08-15)
- **데모가 진짜 파일을 만든다** — `--demo`가 `%APPDATA%\ETReport\demo`에 데모
  DuckDB·리포메터·plot/table 템플릿(xlsx 두 시트 + csv)·실험 조건·안내 문서를
  만들어 [분석] 설정과 [데이터] 프리셋에 꽂는다. 화면은 바로 뜨고, [적용](F5)을
  누르면 그 파일들을 실제로 읽어 같은 화면이 다시 그려진다. 데모 설정은 종료 때
  저장하지 않는다(사용자 `settings.json`에 데모 경로가 남지 않게).
- **데모 DB는 실제 파이프라인으로 만든다** — 합성 raw long → 리포메팅 →
  `pivot_and_load`. 그래서 §10.1 step_seq 병합·retest 최신만·온도 5단위 보정·
  ABSOLUTE 재적용이 데모에서 진짜로 걸린다.
- **데모 데이터에 함정을 담았다** — lot 4개·wafer 43장(한 lot은 25장 → 표 넘침),
  step 2종·온도 3종·site 2종, DC는 `step_seq=1`·누설은 `2`, 음수로 기록되는
  PMOS·누설, NULL·0 분모, REAL 13 · ADDP 12(ABS SQRT LN LOG10 EXP MIN MAX AVG
  SUM STD 전부 + ADDP 사슬), 리포트 2종·CAT 4단·trend(W·L)·Mode 4종,
  미배정 wafer·혼입되는 factor·미리 찍어 둔 제외 2건.
- **사내 소스도 데모로 돈다** — `demo_sources`가 조회 **입구만**(`extractor._fetch`·
  `metrology.fetch`·`fabtracking.fetch`·`s3.client`) 갈아 끼운다. SQL 조립·청크
  분할·스키마 정규화·리포메팅·적재·통계·트리 탐색은 전부 실제 코드가 돈다.
  기간·lot·step 조건을 바꾸면 데모 결과도 따라 좁아진다.
- **csv·tsv를 정식 입력으로** — `xlio`가 csv/tsv를 Excel 없이 읽고 쓴다(열 타입
  규칙은 엑셀 경로와 동일, 앞이 0인 코드는 문자열 유지). 예시 파일이 CSV로
  떨어지는데 정작 다시 읽을 수 없던 비대칭을 없앴고, Excel 없는 PC에서도
  리포메터·템플릿을 열 수 있다. 파일 고르기 필터에도 csv를 넣었다.
- **파일 없이 검증하는 입구** — `reformatter.from_frame()`·`templates.from_frames()`.
  데모·테스트가 파일 경로 없이 `load()`와 **같은 검증 코드**를 탄다.
- 새 도구 `tools/make_demo.py`(앱 없이 번들만 생성), 새 플래그 `--demo-dir`·
  `--demo-rebuild`·`--demo-lite`.
- 새 테스트 `tests/test_demo.py`(데모 계약 25건)·`tests/test_xlio_csv.py`(8건).

### 화면 리디자인 — "계측기 콘솔" + UX (2026-08-15)
- **시각 토큰을 한 곳으로** — `ui/theme.py`의 `TOKENS`가 색·모서리·글자 크기를
  전부 갖고, `style.qss`는 `%TOKEN%` 자리만 쓴다. `tests/test_theme.py`가 치환
  누락과 대비를 이 표 기준으로 검사한다(색을 코드에 박으면 검사가 무의미해진다).
- **크롬과 측정면을 나눴다** — 상단바·도크는 무채색 그래파이트, 카드·표·캔버스는
  흰색. 액센트는 딥 틸 하나. 차트가 이미 Okabe-Ito 8색과 규격 빨강을 쓰므로 UI가
  같은 색조로 경쟁하지 않게 한 결정이다. 차트 자체는 손대지 않았다("화면=PPT").
- **Fusion 스타일 고정 + QSS 확장** — 예전에는 스크롤바·체크박스·콤보 화살표만
  OS 기본으로 그려져 한 화면에 두 시대의 위젯이 섞였다. 스크롤바(얇게·화살표 없이),
  체크박스, 콤보 팝업, 메뉴, 툴팁, 표 헤더, 탭 인디케이터, **키보드 포커스 링**을
  전부 QSS가 덮는다. 콤보 화살표·체크 아이콘은 토큰 색으로 부팅 때 만들어 쓴다.
- **대비(WCAG AA) 정리** — 보조 문구 `#a1a1a6`(2.3:1)과 주 버튼 `#0071e3`(4.32:1)이
  모두 미달이었다. 새 팔레트는 본문·보조·주 버튼·미적용까지 전부 4.5:1을 넘는다.
- **머리가 두 줄이던 문제** — 네이티브 메뉴바(템플릿·도움말)를 감추고 같은 QMenu를
  상단바 버튼에 걸었다. 액션 트리는 그대로라 기존 테스트도 그대로 통과한다.
- **상태 레일** — 상단 오른쪽에 램프(●적용됨 ◐미적용 ○미연결)와 DB·item·포인트·
  제외 수를 모노로 띄운다. 진행 중인 추출 단계도 여기로 올라온다.
- **단축키** — Ctrl+1/2(화면), F5(적용·추출), Ctrl+Enter(보고 있는 탭의 주 동작),
  Ctrl+Z(제외 되돌리기), Esc(중지), F1(설명서). [도움말] → [단축키]에 정리.
- **미적용 표시에 글자를 더했다** — 색만으로 알리던 dirty 상태에 라벨 끝 `•`를
  붙인다(색각 이상·흑백 캡처에서도 보이게).
- **도크 정리** — 파일 5행을 라벨/값 2열로 바꾸고(공백 문자 정렬을 버렸다),
  자주 안 쓰는 S3·계측·SQL·캐시는 [도구]로 접었다(펼침 상태는 설정에 남는다).
- **데이터 화면 2열** — 1500px 창에서 880px 열 하나만 쓰고 좌우가 비던 배치를
  폭 1180px 이상에서 2열로 편다. 실행 버튼이 진행 단계명으로 바뀌던 동작을 없애고
  (버튼은 동작만, 단계는 라벨·레일이) 빈 로그 콘솔에 안내 문구를 넣었다.
- **모달 대신 인라인 알림** — 캐시 비움·예시 저장처럼 확인만 받던 창을
  `ui/widgets/toast.py`의 알림 한 줄로 바꿨다. 실패는 그대로 모달이다.
- **사용 설명서 다시 씀** — 새 화면 캡처로 갈고, '화면 읽는 법'과 '단축키' 절을
  더했다. 표지·본문도 같은 토큰으로 다시 그렸다(번호·차례·쪽 번호).
- 리포트 인스펙터의 '생성될 덱' 마지막 줄이 잘리던 것과, 보조 버튼 오른쪽에
  회색 얼룩이 남던 것(그림자 효과가 평소에도 켜져 있었다)을 고쳤다.
- 회귀 테스트 `test_theme.py`(토큰 계약)·`test_ux_redesign.py`(단축키·레일·알림)
  두 파일을 새로 두었다.

### 사용자 요청 15건 (2026-08-15, 4차)
- **DuckDB "different configuration" 연결 오류** — 분석 화면이 그 DB를 읽기
  전용으로 붙잡은 채 적재하면(특히 신규 item_id를 붙인 같은 lot을 다시 적재할 때)
  DuckDB가 연결을 거부한다. ① 추출을 시작하기 전에 `loader.close_store()`로
  읽기 연결을 먼저 닫고 ② 쓰기 연결은 실패하면 `gc` 한 번 후 재시도하며
  ③ 그래도 안 되면 **무엇을 해야 하는지 적힌 안내**로 바꿔 올린다
  (`loader.explain_conn_error`). 읽기 전용 연결은 전부
  `loader.open_readonly()`(같은 `readonly_config`)를 쓴다 — 설정이 하나여야
  같은 파일을 여러 곳에서 열 수 있다
- **S3 폴더 트리가 비던 문제** — 사내 버킷은 `list_objects_v2`가 `8nm_sram/`·
  `17lpv/`처럼 **버킷 바로 아래** 폴더를 준다. namespace를 키 접두어로 붙여
  조회하던 것을 걷어냈다(namespace는 계정 경계일 뿐 키가 아니다). 정말로
  `<namespace>/` 아래에 넣는 버킷은 [연결]할 때 `detect_root_prefix()`가
  감지해 `root_prefix`로 기억한다
- **SQL 조회의 `Out of Memory Error: Arrow buffer failed to allocate`** —
  결과 전체를 파이썬에 올리던 것을 없앴다. 미리보기는 `LIMIT 200`만 읽고
  행 수는 `count(*)`로 따로 세며, CSV·parquet 저장은 DuckDB `COPY`가 **파일로
  직접** 쓴다(메모리 경유 없음). 읽기 전용 연결에 `temp_directory`를 줘서
  큰 조회가 디스크로 흘러가게 했다
- **정규식 조건의 띄어쓰기** — `P040 L040 P049`를 `P040|L040|P049`로 잇는다.
  RE2에서 공백은 문자 그대로라 예전에는 아무것도 안 잡혔다. 대괄호·괄호·
  역슬래시가 있는(=공백이 패턴의 일부일 수 있는) 입력은 손대지 않는다
- **fab tracking 자동 조회가 [적용]에 반영되지 않던 문제** — 결과를 matrix로만
  들고 있어서 창을 닫으면 사라졌다. 이제 **붙여넣기 표(TSV)로 되돌려** 넣으므로
  파일·붙여넣기와 같은 경로로 저장·재적용된다. 기준(REF) 코드는 'Base'가
  아니므로 `AnalysisConfig.split_baseline`에 함께 저장한다
- **계측·fab tracking 조회에 진행 창** — 사내망 왕복이 수 초씩 걸리는데 창이
  얼어 있었다. `run_in_background`로 넘기고, bdq가 없을 때는 Excel 안내가 아닌
  제대로 된 문구가 나가게 `worker.bdq_call()`을 거친다
- **그룹 스타일 [모든 plot에 적용]** — 고른 그룹의 심볼·크기를 모든 그룹에
  건다(색은 그룹을 구분하는 값이라 그대로). 그룹 스타일이 슬롯마다 따로인 줄
  알고 매번 다시 고르던 흐름을 없앴다
- **점 표시 [모든 plot에 적용]** — 리포트 구성 인스펙터의 점 모드를 모든
  페이지·슬롯에 한 번에 건다
- **Summary CAT1 접기** — 카드마다 ▾/▸ 토글과 [모두 접기]/[모두 펼치기].
  값은 그대로 두고 **보이기만** 바꾸므로 다시 계산하지 않는다
- **온도 보정을 적재 시점으로** — `extractor.correct_temperature()`가 추출
  직후 5단위로 맞춰 **DuckDB에 보정된 값이 저장된다**. 반올림은 DuckDB `ROUND`와
  같은 규칙(0.5는 0에서 먼 쪽)이라 조회 시점 보정(`compat.temp_expr`)과 값이
  같고, 반올림은 멱등이라 raw로 적재해 둔 예전 DB도 그대로 맞는다
- **wafer 표기 통일** (`model/wafers.py`) — `W01` · `W1` · `01` · `1`이 모두
  같은 wafer다. 실험 조건 배정·손 배정·멀티 lot 붙여넣기·inline 계측 조인이
  전부 이 정규화 키로 비교한다(예전에는 `W{int(wf):02d}` 같은 제각각 규칙이라
  표기가 하나만 달라도 배정이 조용히 비었다)
- **멀티 lot 붙여넣기의 머리글은 선택** — `parse_group_rows()`가 첫 줄에
  lot/wafer/group 낱말이 있으면 머리글로 보고, 없으면 첫 줄도 자료로 읽는다.
  머리글이 있으면 **열 순서가 달라도** 된다
- **실험 조건 창이 뜨자마자 죽던 문제** — `textChanged`를 위젯을 만들기 전에
  연결해 놓고 저장된 내용을 채워서, 아직 없는 `self.preview`를 건드렸다
  (`AttributeError: 'SplitSourceDialog' object has no attribute 'preview'`).
  연결을 위젯 조립 뒤로 옮겼다
- **빈 슬롯에 plot 직접 만들기** — 리포트 구성에서 슬롯을 고르고 X·Y만 적으면
  [이 슬롯에 plot 만들기]로 산점도가 생긴다(X가 W·L이면 trend). X·Y 입력칸은
  이제 리포메터 ALIAS 자동완성이 붙는다
- **자동완성이 구분자를 무시한다** — `_`·`-`·공백·`.`을 지우고 비교하므로
  `idsatn`으로 `Idsat N SVT`가 뜬다. `*`는 '사이에 뭐가 있어도 된다'
  (`Ids*svt`). 거르기를 우리가 하므로 Qt가 한 번 더 거르지 못하게 했고,
  Tab 확정이 쉼표 앞 토큰을 두 번 붙이던 버그도 함께 고쳤다
- 테스트 `test_ux_requests3.py` 42건 추가

### 사용자 요청 3건 + 설명서 (2026-08-13, 3차)
- **plot별 점 표시(site/avg/med/std)를 화면에서 고른다** — 데이터·템플릿
  (`Mode` 열)에는 이미 있었으나 UI에 노출이 없었다. 탐색 탭 축 카드와 리포트
  구성 인스펙터에 콤보를 달았다(목록은 `specs.POINT_MODES` 하나가 기준).
  med을 고르면 wafer마다 한 점만 찍힌다(데모: 168점 → 7점)
- **PPT 뒤쪽에 그룹별 평균 표 페이지** — `plot → 표 → **그룹별 평균 표** →
  (유의 인자) → 제외 이력` 순. 그룹이 없으면 만들지 않는다
- **Summary에 '그룹별 wafer' 추가** — 열은 wafer이되 **그룹 머리글로 묶어**
  그룹 순서로 정렬한다. 숨긴 그룹은 빠지고, 그룹이 없으면 lot 머리글로 되돌아간다
- **콤보 팝업이 선택 뒤에도 남던 문제(재발)** — `defer`만으로는 부족했다.
  `tabs/common.on_combo()`가 선택 즉시 `hidePopup()`으로 목록을 내리고 60ms 뒤
  처리를 시작한다. 이미 닫힌 창의 지연 처리는 조용히 건너뛴다
- **템플릿 예시·설명 최신화** — plot 예시에 `Type=trend · x=W · Mode=med` 행을
  넣고, Mode 설명을 값별로 풀어 썼다. 실험 조건 설명에 [fab tracking 자동],
  table 설명에 표 종류 4가지(평균·산포·그룹별 평균·그룹별 wafer)를 덧붙였다.
  로더가 요구하는 컬럼과 예시 컬럼을 대조해 **누락 없음**을 확인했다
- **사용 설명서 PDF를 앱에 심었다** — `tools/make_manual.py`가 데모 모드를
  offscreen으로 띄워 화면을 찍고, `export/manual.py`가 QPdfWriter로 9쪽 PDF를
  만든다(외부 의존성 없음, 한글 그대로). [도움말] → [사용 설명서 (PDF)]가 이
  파일을 연다. 패키지 데이터와 PyInstaller 번들에 함께 들어간다
- 화면 확인에서 잡은 것: 슬롯 미리보기의 축 이름·눈금이 잘리던 문제(캔버스
  크기 동기화 + 리사이즈 후 재렌더), 축 카드 안내 문구가 카드 밖으로 넘치던 것,
  그룹별 평균 카드의 [xlsx] 버튼 누락
- 테스트 `test_ux_requests2.py` 15건 · `test_manual.py` 7건 추가. Qt 지연 처리에
  기대던 단언은 `conftest.qt_until()` 조건 대기로 바꿔 흔들리지 않게 했다

### 사용자 요청 8건 (2026-08-13)
- **trend의 X축 규격 세로 점선 제거** — 규격은 y값의 한계라 WIDTH·LENGTH 위치에
  그으면 의미 없는 격자만 늘어난다. 타깃(파란 X)은 그대로 둔다
- **리포트 구성 탭에도 그룹 스타일 UI** — 색·심볼·크기·REF. 탐색 탭의 카드를
  `ui/widgets/style_card.py`로 빼서 **두 탭이 같은 위젯**을 쓴다(규칙 중복 없음)
- **템플릿 예시를 한 파일에 시트로** — [4종 한 파일로 저장]이
  `sample_templates.xlsx` 하나에 `리포메터 / 리포메터 설명 / …` 8시트로 만든다.
  Excel이 없으면 종류별 CSV로 폴백. 예시 리포메터의 기하 열을 **WIDTH·LENGTH**로
  바로잡았다(로더가 읽는 이름과 일치)
- **콤보에서 항목을 골라도 목록이 남던 문제** — 선택 핸들러가 그 자리에서 무거운
  일을 하면 팝업 닫힘 이벤트가 밀린다. `tabs/common.defer()`로 한 박자 미뤄
  팝업이 먼저 닫히게 했다(스케일·그룹·크기·설정 프리셋·표 모드 콤보)
- **탐색 탭에서 X에 W·L을 넣으면 아무것도 안 그려지던 문제** — 탐색은 템플릿의
  Type 열이 없어 항상 scatter로 그렸다. 이제 X가 기하면 자동으로 trend로 전환
- **PPT 표지 장표 추가** — LINE_ID·ROOT_LOT_ID·STEP_ID·TEMPERATURE·TKOUT_TIME
  (마지막 측정 기준). 실험 조건이 있으면 **다음 장에 조건 정리 표**가 들어간다
- **PPT plot 해상도·글자 크기 상향** — 이미지 150→**220dpi**, 축 이름·눈금·제목
  글자를 키웠다(하한 6→8pt, 상한 9.5→13pt). 화면 캔버스도 같은 함수를 쓴다
- **Summary에 '그룹별 평균' 추가** — 열이 wafer가 아니라 **그룹**이 된다.
  화면·복사·xlsx·PPT가 같은 `build_table` 결과를 쓴다
- 회귀 테스트 `tests/test_ux_requests.py` 19건 추가. 그 과정에서 **x·y에 같은
  item을 넣으면 polars DuplicateError로 죽던 버그**(§10.10)를 함께 고쳤다

### 분석 화면 버그 9건 (버그수정_9건 프롬프트)
- **1·9 배포 exe에서 터지던 ImportError/TypeError** — `pptgen.table_mode_of`와
  `build_deck(tables=…)`가 이미 들어와 있음을 확인하고 회귀 테스트로 고정했다.
  **현장 exe는 재빌드가 필요하다**
- **2 온도가 23.9·149처럼 나오던 문제** — 읽는 시점에 **5의 배수 정수**로
  보정한다(`compat.temp_expr`). 병합 키·필터 콤보·배정 비교가 모두 같은 값을
  쓰도록 ctx_select·long PIVOT 그룹·merge inner·wafer_index 네 곳에 전부 적용.
  적재는 손대지 않아 기존 DB도 화면·PPT가 맞는다
- **3 [적용] 시 Summary·Report가 자동으로 그려지던 문제** — 신호별로 정책을
  나눴다. `data_changed`·`report_changed`·`explore_changed`는 dirty만,
  `groups_changed`(그룹 토글)는 즉시 반영(`StaleMixin.refresh_if_visible`)
- **4 그룹 편집에서 DB를 바꿔도 이전 배정이 남던 문제** — 다른 DB면
  `manual_groups`를 비우고 자동 그룹핑을 새 DB 기준으로 다시 돌린다
- **5 자동 그룹핑 추가** — 그룹 편집 창에서 wafer별/lot별을 골라 실행한다.
  이름은 wafer(또는 lot) ID, 첫 그룹이 REF, 재실행은 멱등. [적용] 전에도 된다
- **6 [적용] 전에 짠 그룹이 초기화되던 문제** — `apply_config`가
  `load_state` 뒤에 `_apply_split`을 또 불러 손배정을 덮어쓰고 있었다. 중복
  호출을 없애고, factor 편집 경로에서는 split 배정 뒤 manual을 다시 건다
  (사용자가 고른 쪽이 이긴다). 손으로 만든 그룹 정의도 보존
- **7 병목 버튼에 로딩 표시** — [적용]은 워커 스레드 + 진행 창(실패해도 잠금
  해제), SQL 조회·CSV/parquet 저장도 백그라운드. **[그리기]/[미리보기]는
  워커 렌더를 쓰지 않는다** — matplotlib이 폰트·텍스트 메트릭 캐시가 스레드
  안전하지 않아 QThread에서 그리면 프로세스가 abort한다(확인함). 대신 버튼
  잠금 + "그리는 중…" + 대기 커서로 처리(Q7-4 폴백 B)
- **8 표에 그룹 배정이 반영되지 않던 문제** — `wafer_columns()`가 plot과 같은
  기준(visible 그룹)으로 좁힌다. 화면 Summary·복사·xlsx·PPT가 같은 열을 쓴다
- 회귀 테스트 `tests/test_bugfix_9.py` 24건 추가

### 신규 기능 3종 (추가기능구현 프롬프트)
- **A. fab tracking 자동 split grouping** (`data/fabtracking.py`) —
  `fab.f_fab_tracking`에서 `line_id='KFBK'` + `ein_ecn_no IS NOT NULL`로 split
  실험 lot을 찾고, **area='PHOTO'는 recipe(reticle_id)·그 외는 ppid**로 step별
  조건을 비교해 **갈리는 step만** 실험 축으로 올린다. 그룹핑·혼입 감지는
  `model/split.SplitMatrix`를 그대로 쓴다(중복 구현 없음). 멀티 lot 지원.
  컬럼명은 원본 그대로. 실험 조건 창의 [fab tracking 자동] 버튼으로 진입
- **B. inline 계측 분석 활용** (`data/metrology.py` + 계측 창) —
  `fab.f_fab_wf_met`를 **분석 중인 lot에 한해** 조회해 (lot, wafer)로 붙인다.
  site level은 `RANGE/STD/MIN/VALUE/SLOTID/Q2/MAX` 제외, wafer level은 `Q2`.
  붙인 계측은 탐색 X축·Summary에서 바로 쓰고, `top_factors()`가 상관(r)과
  그룹 간 유의차(Welch t)로 **top-k 인자**를 뽑아 PPT 슬라이드로 내보낸다
  (`item_id REGEXP 'CD|THK|DEPTH|TIP|RCS'`). wafer 집계는 `model/aggregate`
  재사용, 계산은 버튼 트리거(지연 계산 규약 유지)
- **C. S3 저장소** (`data/s3.py` + S3 창) — namespace·bucket·AccessKey·Secret
  4항목 로그인, 폴더 트리(Delimiter로 한 단계씩), duckdb·csv·sbdf·parquet
  업로드/다운로드. 느린 작업은 전부 백그라운드. **Secret은 settings.json에
  평문으로 두지 않는다** — 별도 파일(`%APPDATA%\ETReport\s3_credentials.json`,
  POSIX 0600)에 두고 저장 여부를 사용자가 고른다. boto3는 선택 의존성
  (`pip install -e ".[s3]"`)이라 없으면 그 창만 안내를 띄운다
- 테스트 `test_fabtracking.py` 16건 · `test_metrology.py` 22건 ·
  `test_s3.py` 22건 추가 (bdq·boto3 없이 전부 돈다)

### item_id 청크 — 병렬 단위·진행 표시·미리보기 (사양 §4.2 · §4.3 · §10.10)
- **병렬 단위가 날짜뿐이던 문제** — item 그룹은 그 안에서 순차라 3그룹이면 실질
  병렬도가 1/3이었다. 이제 `plan_units()`가 **기간 × item 그룹의 곱**을 펴서
  그 전체를 4워커가 나눠 문다(사양 예시대로 item 24,180개 · 7일 → **21청크**)
- **진행 표시에 item 그룹이 없던 문제** — 로그가 "청크 7개"로 떴다. 이제 청크 수가
  곱이고, 진행 라벨이 `08-04 item 2/3`이라 어느 그룹에서 시간이 가는지 보인다.
  추출 시작 전에 `조회 item 24,180개 (REAL만, ADDP 제외) — 상한 초과로 3개 그룹
  분할` 한 줄을 남긴다. parquet 파일명 접미사도 `_g1…_g3`으로 바꿨다
- **SQL 미리보기가 item 목록을 통째로 그리던 문제**(§10.10) — 24,180개면
  **435,576자**를 조건 고칠 때마다 다시 그렸다. `build_preview_sql()`이
  `item_id IN ( … 24,180개 … )` 자리표시자와 개수·그룹 수 주석만 만든다
  (**478자 · 0.1ms**). 실제 목록은 실행할 때만 만든다
- 회귀 테스트 `tests/test_chunk_plan.py` 16건 추가 — 그룹 경계(9999/10000),
  곱 단위 제출·진행률·파일명, **쿼리 하나당 item 수가 상한 이하이고 합계가
  정확히 원래 개수**인지, 미리보기가 목록을 만들지 않고 조건은 실행용과 같은지

### 도크·메뉴·실험 조건 입력 (사양 §5.1 · §11.4 · §3.4 · §14)
- **REPORT 콤보 제거**(§5.1 확정) — 리포트는 고르는 것이 아니라 템플릿의 Report
  열이 정한다. 적용된 이름을 문구로만 보여 주고, 템플릿에 리포트가 여럿이면
  "리포트 N개 (…) — 다른 리포트를 쓰려면 템플릿의 Report 열을 바꾸세요"로 안내
- **[템플릿]·[도움말] 메뉴 추가**(§11.4) — 리포메터·plot·table·실험 조건 4종
  예시를 내려받는다. 각 파일에 **'설명' 시트**(컬럼·의미·예시·규칙)가 함께 들어가고,
  Excel이 없거나 저장에 실패하면 **CSV + 설명 CSV로 폴백**한다. [도움말]에는
  네 파일이 어떻게 맞물리는지 관계도를 넣었다. 예시 리포메터는 실제 로더 검증을
  통과하고(ADDP 2개 계산 가능), table 예시는 CAT4까지 넣어 계층 확장을 보여 준다
- **실험 조건 입력 3경로**(§3.4 확정) — [실험 조건]이 전용 창으로 바뀌어
  엑셀 파일 / CSV·TSV / **클립보드 붙여넣기**를 한 창에서 고른다. 붙여넣으면
  **즉시 파싱해** 미리보기와 step 목록을 보여 주고, 붙여넣은 내용은 프리셋에
  남아 [적용] 때 다시 읽힌다(`AnalysisConfig.split_text`)
- 붙여넣기 파서의 **빈 칸이 별개 조건 코드가 되던 문제** — `''` 대신 None으로 둬야
  `fill_null(baseline)`이 먹는다. 그대로 뒀다면 빈 칸짜리 가짜 그룹이 생겼다
- **표에 배정된 lot만 나온다**(§14) — `wafer_columns()`가 gid가 붙은 wafer만
  돌려준다. 배정이 하나도 없을 때만 전체를 보여 준다(그룹을 안 쓰는 흐름)
- 회귀 테스트 `tests/test_dock_and_samples.py` 19건 추가

### 탐색 탭 오른쪽 패널 카드 2종 (사양 §5.2)
- **"스케일 · 범위" 카드 추가** — X/Y 각각 자동·log·선형을 고르고, [범위 직접
  지정]을 켜면 **지금 보이는 축 값이 그대로 채워진다**(빈칸에서 시작하면 자릿수를
  손으로 옮겨 적어야 한다). 값은 렌더러가 실제로 쓴 축 범위를 읽어 오므로 축 규칙을
  두 곳에 두지 않는다. 숫자가 아닌 입력은 비우고 자동으로 되돌린다
- **"그룹 스타일" 카드 추가** — 그룹을 고르고 색·심볼·크기·REF를 바로 지정한다.
  REF는 하나만 유지된다. 편집은 `groups_changed` **한 방향**으로만 알리고, 그
  신호를 되받아 컨트롤을 갱신할 때는 시그널을 막아 재귀를 피한다(§5.2 동기화 주의).
  그룹이 0개면 컨트롤이 비활성이라 인덱스를 직접 쓰지 않는다(§10.9)
- 회귀 테스트 `tests/test_explore_cards.py` 16건 추가
- **REF μ±3σ 밴드(사양 §6·§5.2)는 넣지 않았다** — 2026-08-11에 전체 plot에서
  삭제하기로 확정된 사항이라(`docs/trend-chart-plan.md`) 사양서 쪽 문구가 낡았다.
  사양서만 보고 되살리지 않도록 CLAUDE.md에 근거를 적어 뒀다

### CAT 계층 개수 고정 해제 (사양 §3.3)
- **CAT4·CAT5를 둬도 무시되던 문제** — 템플릿 로더가 CAT1~CAT3만 읽었다. 이제
  정규식 `CAT\d+`로 찾아 **번호순**으로 전부 읽는다(CAT10도 순서가 맞는다)
- **CAT3 열이 없는 템플릿이 '컬럼 누락'으로 중단되던 문제** — 필수 컬럼은
  `item_id · CAT1 · Report`뿐이다. CAT2 이후는 있으면 쓰고 없으면 그만
- `TableRowSpec`이 `cats`(CAT1부터 번호순) 하나를 들고, `cat1`(표를 나누는
  기준)·`subcats`(표 안 계층)는 파생 속성이다. 화면·복사(TSV)·xlsx·PPT는
  `TableData.labels()`/`label_values()` 한 쌍으로 라벨 열을 만든다 — 열 개수를
  코드에 박아 둔 곳이 없어졌다
- 표 헤더에 **템플릿의 CAT 열 이름을 그대로** 쓴다(예전에는 "CAT2/CAT3" 고정).
  이름이 없으면 CAT 번호로 채운다
- 세로 병합 키에 상위 CAT을 포함해 **상위가 바뀌면 하위 병합이 끊긴다**(§3.3).
  xlsx도 CAT2만 병합하던 것을 모든 CAT 열로 넓혔다
- PPT 표의 라벨 열 폭이 CAT 개수를 따라간다(`label_widths_in`)
- 회귀 테스트 `tests/test_cat_levels.py` 11건 추가 — CAT5 템플릿이 화면·복사·
  PPT에서 모두 렌더링되는지(§14 체크리스트)

### 그룹 편집 다이얼로그 (사양 §9.1)
- **[적용] 전에는 [조회]가 아무 일도 하지 않던 문제** — `state.data`(=[적용] 후
  프레임)만 보고 있었다. 이제 창에 [DB 선택]이 있고, 경로만 있으면 그 자리에서
  **읽기 전용으로 열어 조회**한다. item 컬럼을 만지지 않는 가벼운 색인
  질의(`compat.wafer_index_sql`)라 큰 DB에서도 빠르다 — 대부분의 사용자가
  "DB만 고르고 그룹부터 짜는" 흐름을 쓴다
- **4단 연쇄 필터 추가** — `lot → step_id → total_site_cnt → temperature`.
  앞 단계를 좁히면 뒤 콤보 목록과 `유효 N장 · M포인트`가 따라 바뀐다
- **배정도 필터 범위에만 적용**한다. 같은 wafer라도 step·온도·site가 다르면
  다른 측정점이므로 M5만 골라 배정하면 M2 측정점은 그대로 미배정으로 남는다
- **lot 찾기를 3단계로** — 정확히 일치 → 대소문자 무시 → 부분 일치. 못 찾으면
  비슷한 lot을 예시로 보여준다(예전에는 대문자 정확 일치만)
- **손으로 짠 그룹이 [적용] 때마다 사라지던 문제** — 이제
  `state.manual_groups`에 남고 로딩 후 다시 붙는다(실험 조건 배정보다 나중에
  걸어 사용자가 고른 쪽이 이긴다). 멀티 lot 붙여넣기도 같은 경로를 쓴다
- 분석 프레임에 측정 조건 컬럼 `step·temp·site`를 함께 싣는다
  (`loader.RESERVED` 확장). 없는 스키마에서도 NULL로 자리를 만들어 모양이
  흔들리지 않는다
- 회귀 테스트 `tests/test_group_dialog.py` 9건 추가

### 리포메터 셀 해석 (사양 §3.1)
- **ABSOLUTE를 `TRUE`나 `1`로 적으면 절대값이 조용히 무시되던 문제** — `== "Y"`
  하나로 판정하고 있었다. 이제 확정 사양대로 `TRUE/T/Y/1/O`가 참,
  `FALSE/N/0/X/빈칸`이 거짓이다(대소문자 무관, 엑셀 체크박스의 bool과 1.0/0.0도
  처리). 모르는 값은 거짓으로 두되 **경고를 남긴다**(예: `ABSOLUTE '예'를 …`)
- **ADDP 함수 `LOG`·`EXP`가 없어 그 행이 통째로 빠지던 문제** — 사양의 함수
  목록(`ABS SQRT LN LOG LOG10 EXP MIN MAX AVG SUM STD`)을 채웠다.
  **`LOG`는 상용로그(= LOG10), `LN`은 자연로그** — 엑셀 관례
- **`Exp({A}/{B})`에서 분모가 0이면 벡터 경로만 0이 나오던 문제** — polars는
  0으로 나누면 ±inf를 주는데, 최종 결과에서만 걸러 내다 보니 `exp(-inf) = 0.0`이
  멀쩡한 값으로 남았다(행 단위 경로는 NULL). 나눗셈·거듭제곱 자리에서 바로
  NULL 처리한다. Exp 오버플로도 양쪽 모두 NULL
- `test_reformatter_vector.py`의 "모든 화이트리스트 함수가 벡터화됐는가"를
  손으로 적은 목록 대신 `_BASE_FUNCS`에서 뽑도록 바꿨다 — 함수를 추가하면서
  벡터 경로를 깜빡하면 실패한다

### PPT 슬라이드·표 페이지 (사양 §7 · §10.5)
- **표 때문에 덱 전체가 넓어지던 문제** — wafer가 많으면 슬라이드를 최대 56인치까지
  늘려서 plot 페이지까지 가로로 늘어졌다(wafer 25장이면 비율 2.87). 이제
  **항상 16:9(13.333 × 7.5)** 고정이고, 넘치는 것은 표만 넘긴다
- **표가 PPT에 아예 안 들어가거나, 실험마다 중복되던 문제** — 표를 plot 슬롯
  (`Type=table`)에서만 그렸다. 사양대로 만든 템플릿에는 그 행이 없으므로 표가
  한 장도 없었고, 옛 템플릿이면 실험 수만큼 같은 표가 반복됐다. 이제 표는
  **전용 페이지**로 CAT1마다 한 장, 순서는 `plot 전부 → 표 전부 → 제외 이력`.
  `Type=table` 행은 무시한다(슬롯을 비운다)
- **lot 헤더가 `PA1\nPA1\nPA1`로 나오던 문제(§10.5)** — 값을 먼저 쓰고 병합하고
  있었다. 병합 먼저·값 나중으로 바꿨다. CAT2/CAT3 세로 병합도 마찬가지이며,
  **상위 CAT이 바뀌면 하위 병합을 끊는다**(§3.3)
- 표 모드 이름을 사양과 맞췄다 — `wide` → **`overflow`**(기본, 9pt 유지하고
  표가 슬라이드 밖으로 이어짐) · `split`(wafer 12장씩, 제목에 1/N). 예전
  설정 파일의 `wide`는 overflow로 읽는다
- 표 스타일을 §7.4에 맞춰 다시 짰다 — 기본 파란 줄무늬 제거, 회색 헤더, 교대 행,
  행마다 얇은 가로선·헤더 아래만 진한 선, 값 열 우측 정렬, 규격 이탈 셀만 붉게.
  글자는 문단·run 양쪽에 9pt를 박아 뷰어 기본값으로 커지지 않게 했다
- PPT 표가 Summary 탭의 평균/산포·Δ 선택을 그대로 쓴다(예전에는 평균 고정) —
  "화면 = 출력" 원칙
- 회귀 테스트 `tests/test_pptgen_deck.py` 16건 추가

### ABSOLUTE가 안 먹는 것처럼 보이던 문제 (사양 §10.2)
- **이미 음수로 적재된 DB는 리포메터를 고쳐도 그대로이던 문제** — 절대값이
  추출 시점에만 걸려 있었다. 이제 분석 로딩·[적용] 시점에도 다시 건다
  (`loader.apply_absolute`). 절대값은 멱등이라 몇 번을 걸어도 값이 같다
- **스케일은 재적용하지 않는다** — 멱등이 아니라 다시 곱하면 배율만큼 어긋난다.
  회귀 테스트로 고정
- 도크 요약에 `· 절대값 N`을 표시해 값이 왜 바뀌었는지 화면에서 보이게 했다
- 원본 DB는 그대로다(읽기 전용) — 교정은 화면에 올리는 프레임에서만 일어난다

### 문자열 tkout_time이 조용히 전부 null이 되던 문제 (사양 §10.4)
- **추출 결과의 시각이 문자열로 오면 `cast`가 예외 없이 전부 null로 만들던
  문제** — `str.to_datetime()` 파싱으로 바꿨다. tkout_time이 null이면 key_hash가
  뭉쳐 서로 다른 측정이 중복으로 지워지고 retest 최신 판정도 무의미해진다
- **Categorical로 온 숫자 컬럼**(bdq/pandas 경유)은 Utf8을 한 번 거쳐 캐스팅한다
  — polars가 Categorical → 숫자 직접 캐스팅을 막는다
- 조회 결과에 없는 컬럼은 null로 채워 청크 parquet 스키마를 고정했다. 청크마다
  컬럼이 다르면 `scan_parquet`로 함께 읽는 적재가 깨진다
- 위 세 가지를 `extractor.normalize_schema()` 한 곳으로 모았다

### step_seq 때문에 산점도가 비던 문제 (사양 §10.1)
- **x가 `step_seq=1`·y가 `2`에 기록된 데이터에서 plot이 통째로 비던 문제** —
  분석 로딩이 seq만 다른 행을 합치지 않아 x·y가 함께 있는 행이 0개였다. 이제
  `lot·wafer + die 좌표·온도·step_id·site_cnt`로 묶어 한 측정점으로 합치고
  각 item은 NULL이 아닌 값을 취한다. **step_id·온도·site_cnt가 다르면 합치지
  않는다**(다른 측정점이므로). seq 컬럼이 없는 스키마는 예전 그대로 둔다
- **retest 중복 제거(QUALIFY) 파티션에 `step_seq`가 빠져 있던 문제** — seq가
  다른 정상 행이 '구버전'으로 지워지고 있었다. 파티션에 seq를 포함
- long 테이블도 같은 규칙으로 PIVOT하며, 집계를 `first`(NULL도 이긴다)에서
  `any_value`(NULL이 아닌 첫 값)로 바꿨다
- 합친 행의 `key`는 구성 행 key의 최솟값이라, seq가 하나뿐이던 기존 DB에서는
  키가 그대로다 — 제외 포인트 사이드카가 유지된다. 다만 **실제로 병합이 일어난
  포인트**(지금까지 plot이 비어 있던 경우)의 제외 이력은 새 키로 다시 잡힌다
- 회귀 테스트 `tests/test_step_seq_merge.py` 10건 추가. `tests/factory.py`가
  step_seq를 항상 1로 만들어서 기존 테스트는 이 경로를 한 번도 타지 않았다

### 부팅(app.py) 정리
- **배포 exe에서 오류가 아무 데도 남지 않던 문제** — `--windowed` 빌드에는
  콘솔이 없어 `sys.stderr`가 None이다. 로그를
  `%APPDATA%\ETReport\logs\etreport.log`(1MB × 3 회전)에 남기고, 처리되지 않은
  예외는 excepthook이 로그 + 알림 창(같은 오류는 한 번만)으로 알린다. 앱은
  죽지 않는다
- **카탈로그 캐시가 깨지면 기본 목록으로만 돌던 문제** — 깨진 캐시는 버리고
  bdq 조회로 넘어간다(조회에 성공하면 캐시도 새로 쓰인다)
- CLI 정리 — `--no-update`(시작 시 버전 확인 생략), `--log-level`, `--version`
  추가. `--demo`는 사내 GHE를 부르지 않는다. Qt 옵션(`-platform` 등)은 그대로
  Qt에 넘기고 우리 플래그는 Qt에 넘기지 않는다
- 창이 뜨기 전 설정 — 고DPI 반올림 PassThrough(125%·150% 배율에서 레이아웃이
  튀던 것), 앱 이름·버전 메타데이터, Windows 작업표시줄 AppUserModelID
- 진입점(`app.py`) — Python 3.11 미만·`src/` 없음·의존성 미설치를 트레이스백
  대신 한 줄 안내로. 종료 시 설정 저장 실패가 종료를 막지 않게

### 코드 리뷰 반영 (P0 1 · P1 4 · P2 9 · P3 8)
- **[P0] 개발 실행에서 [지금 업데이트]를 누르면 소스 트리가 지워지던 문제** —
  `install_dir()`이 frozen이 아닐 때 저장소 루트를 돌려줬고, 그 경로가
  `robocopy /MIR`의 대상이 됐다. 이제 exe 배포본이 아니면 적용을 거부한다
  (`UpdateNotApplicable`). 배치도 `/MIR` → `/E`(삭제 없는 복사), ASCII 전용,
  zip 경로 검증 추가
- **[P1] PPT 생성 때마다 matplotlib Figure가 쌓이던 누수** — pyplot 대신
  Figure를 직접 만들고 저장 후 정리. 덱 하나에 수백 장이 남던 것이 0
- **[P1] DuckDB 연결을 닫지 않던 문제** — 적재 Store를 finally에서 닫고
  (안 닫으면 이어지는 [분석] 자동 연결이 잠금으로 실패), [적용]마다 새던
  읽기 전용 연결도 정리(`loader.close_store`)
- **[P1] 리포메팅이 추출 원본 parquet를 덮어쓰던 문제** — `*_rf.parquet`로
  따로 쓴다. 리포메터를 고쳐 다시 돌릴 때 재추출이 필요 없다
- **[P1] Summary [복사]가 Δ vs REF를 무시해 화면과 다른 숫자를 주던 문제** —
  복사·xlsx·PPT가 모두 `build_table`/`to_tsv` 한 경로만 쓴다
- [P2] 추출 중 창을 닫으면 죽던 문제(QThread 정리), 설정·제외·카탈로그 JSON을
  원자적으로 저장(종료 중 크래시로 프리셋이 날아가지 않게)
- [P2] xlsx 시트 이름 정리(`/`·`[]`·31자·중복), 표 0개일 때 방어
- [P2] 템플릿 되쓰기에서 page·order를 숫자로 유지(텍스트로 바뀌던 문제)
- [P2] 제외 클릭 1회당 재계산 축소 — 그룹 수만큼 돌던 필터를 group_by 1회로,
  리포트 슬롯은 캔버스를 재생성하지 않고 다시 그리기만
- [P2] PPT·xlsx를 워커 스레드로(진행 창 표시, COM 초기화 포함)
- [P2] Impala 조건 이스케이프 — 역슬래시, LIKE의 `_`
- [P2] staging parquet 7일 후 자동 정리
- [P2] `analysis_ws.py`(1,236줄)를 `ui/tabs/{explore,summary,report,common}.py`로
  분할, 지연 계산 토글을 `StaleMixin`으로 통합
- [P3] 죽은 코드 제거(`fact_exists`·`already_loaded`·no-op 분기), split 라벨을
  튜플로 다뤄 코드에 구분자가 들어가도 안전, SQL 결과 CSV 스트리밍 저장,
  추출 기간 역전 검증
- 테스트 40개 추가 — 위 수정들의 회귀 테스트 + 데모 데이터로 창을 조립하는
  UI 스모크(headless)

- **리포메터 `Sum()` 이 NULL 대신 0을 주던 문제 수정** — 인자가 전부 미측정이면
  polars가 0을 돌려줘서, 측정이 아예 없는 wafer의 ADDP가 '0'이라는 값으로
  plot·표·Δ에 들어갔다. 행 단위 엔진과 같은 NULL로 통일
- **리포메터 수식에서 `{}`를 빠뜨린 ALIAS를 검증 단계에서 제외** — 예전에는
  검증을 통과한 뒤 추출이 다 끝난 시점의 리포메팅에서 NameError로 전체가
  중단됐다. 이제 그 item만 빼고 "중괄호로 감싸세요" 경고를 남긴다
- **적재 속도** — 20만 행(item 1000) 기준 195초 → **1.4초**, 140만 행 4.6초
  - 버킷마다 lazy를 다시 collect해 key_hash(행 단위 blake2b)를 512번
    재계산하던 것을 한 번만 계산하도록 변경
  - 버킷 수를 512 고정에서 데이터 모양(키 수 × item 수)에 맞춰 정하도록 변경
    (`plan_buckets`). 버킷은 피벗 작업 단위일 뿐이라 DB에 들어가는 내용은
    그대로다 — 예전에 적재해 둔 DB에 이어 적재해도 결과가 같다
- 적재 결과 행수가 실제 삽입 행수를 반영 — 같은 파일을 다시 적재하면 0행으로
  표시된다(예전에는 중복으로 걸러진 행까지 세서 보고했다)
- 테스트 168개 추가(`tests/`) — 리포메터 벡터/행 경로 동치, 시트 스키마 계약,
  템플릿 검증, 적재·조회 불변식. Excel·bdq 없이 전부 실행된다
  (`pytest`, 대용량은 `pytest -m slow`)
- 사내 PC용 testset 생성기 `tools/make_testset.py` — 리포메터 xlsx + 추출 결과와
  같은 모양의 long parquet + 정답표 CSV(행 단위 엔진 계산)
- ruff 설정 추가(`pyproject.toml`)와 기존 지적 사항 정리 (`ruff check .`)
- 리눅스에서 한글이 전부 □로 깨지던 문제 수정 — 폰트 이름을 QSS·렌더러에
  하드코딩(`Malgun Gothic`/`Consolas`)하던 것을 `fonts.py` 한 곳으로 모으고,
  실행 시점에 OS별로 고르도록 변경 (Qt와 matplotlib가 같은 폰트를 쓴다)
  - 설치된 한글 폰트가 없으면 폰트 파일을 직접 찾아 등록.
    WSL은 `/mnt/c/Windows/Fonts`(맑은 고딕)를 그대로 사용
  - `src/etreport/assets/fonts/` 에 폰트를 넣으면 동봉해서 배포(빌드에 포함),
    `ETREPORT_FONT` 로 패밀리·파일 직접 지정 가능
  - 하나도 못 찾으면 설치 방법을 로그로 안내(앱은 그대로 뜬다)
- style.qss: 고정폭 글꼴에 `DejaVu Sans Mono`/`Menlo` 등 비윈도우 대체 추가
- build_release.py: `--add-data` 구분자를 `os.pathsep` 으로 (리눅스 빌드 대응)

## v1.7.3
- 리포메팅 진행 표시: 파일별 행수·소요시간, ADDP item 이름까지 단계 표시
  (리포메터 읽기 / 추출 / 리포메팅 / 적재 각각 초 단위 기록)
- ADDP 계산 벡터화 — 수식을 polars 식으로 번역해 한 번에 계산.
  번역 불가한 수식만 기존 행 단위 엔진으로 폴백(로그에 기록).
  30만 행 × ADDP 5개 기준 0.6초
- 적재 버킷 진행을 2초마다 로그로 남김
- 실행·진행 로그를 화면 하단에 고정 (스크롤과 무관하게 항상 보임)

## v1.7.2
- group_dialog: 그룹이 0개일 때 IndexError로 창이 뜨지 않던 문제 수정
  (인덱스 직접 접근 → _current_group() 경유, 모든 조작에 빈 상태 방어)
- group_dialog: 루프 변수 w가 위젯 변수 w를 덮어써 레이아웃이 파괴되던 버그 수정
- group_dialog: 실험 조건 파일 없이도 쓸 수 있게 그룹 ＋추가 / 이름 변경 / －삭제 추가
  (첫 그룹은 자동으로 REF)
- 리포트 구성 슬롯에서 클릭 제외가 먹지 않던 문제 수정 — 히트테스트 좌표를
  그릴 때 캐시하면 이후 레이아웃 크기 변화로 어긋나므로, 클릭 시점에 변환하도록 변경
  (슬롯은 허용 반경 10px)

## v1.7.1
- catalog: `bdq.columninfo` → **`bdq.getColumnInfo`**
- querybuilder: KEY_COLS를 리스트로 바꾸고 SELECT 절은 쉼표 문자열로 조립
  (f-string에 튜플이 들어가 괄호가 SQL에 섞이던 문제). item_id·et_value 포함
- tkout_time은 항상 문자열 리터럴로 전달 (기간 조건·사용자 TIMESTAMP 조건 모두)
- 리포메터 수식에 ABS 포함 + Sum/Ln 추가, 대소문자 별칭 지원(ABS/Abs/abs)
- 원본 컬럼 et_value를 내부 표준 value로 정규화 (리포메터·적재·호환 계층)

## v1.7.0
- 유효성 검사를 **[적용] 한 번**으로 통합(model/session.py). 파일을 고를 때는
  경로만 담고 Excel/DB를 열지 않음 — 파일마다 재검증하던 병목 제거
- 분석 설정 저장 UI: 이름 붙여 저장·전환·삭제 (DB·템플릿·시트·REPORT·
  로그축 패턴·실험 조건 경로를 한 묶음으로)
- 지연 계산: Summary [표 만들기] · 탐색 [그리기] · 리포트 [미리보기].
  변경되면 버튼이 주황색으로, 보고 있는 화면만 자동 갱신
- 집계를 model/aggregate.py로 통합 — group_by 1회로 전체 wafer 통계 계산
  (화면·xlsx·PPT가 같은 함수를 사용)

## v1.6.0
- Excel 캐시 계층(data/xlio.py): 여러 시트를 한 번에 읽고 parquet로 캐시.
  원본 mtime/size 변경 시 자동 무효화. 도크에 캐시 현황·비우기 버튼
- 리포메터 전용 창: 파일·**시트 전환**, item 목록 미리보기, 검증 결과,
  [새로 읽기](캐시 무시)
- SQL 조회·내보내기 창 복원: 자유 SQL(읽기 전용) → 미리보기 →
  CSV(BOM) / parquet / SBDF 저장. 예시 스니펫 포함
- 리포트 구성에서 점 제외 가능. "모든 plot에서 함께 제외" 옵션 추가
- 슬롯 **드래그&드롭 재배치** + 슬롯 인스펙터(제목·X·Y·로그축·비우기)
- [템플릿에 저장]: 화면 배치를 plot 템플릿 엑셀에 되쓰기.
  다른 Report 행은 보존하고 .bak 백업 생성

## v1.5.0
- DuckDB 테이블 이름을 **et_data** 로 통일 (`select * from et_data` 호환).
  예전 버전이 만든 `fact` 테이블도 계속 인식
- 기존 DB 호환 계층(data/compat.py): 테이블 자동 선택, 컬럼명 별칭 자동 인식,
  long(item_id/value) 테이블은 읽으면서 PIVOT. 스키마가 달라도 조회 가능
- 분석 화면은 DB를 **읽기 전용**으로 연다 — 원본을 절대 수정하지 않음
- 제외 포인트를 DB 테이블에서 사이드카 JSON으로 이동(DB 경로별 저장)
- Summary xlsx 내보내기 실배선 (xlwings 서식 적용)
- Summary 표 행 잘림·카드 겹침 수정

## v1.4.0
- 추출 진행 로그 패널 추가: 단계 표시(리포메터/추출/리포메팅/적재), 진행률 바,
  청크별 완료·행수·소요시간, 로그 복사, 중지 버튼
- settings.json 하위호환: 없어지거나 새로 생긴 필드가 있어도 앱이 죽지 않음
  (log_patterns TypeError 수정). 손상된 파일이면 기본값으로 복구
- 리포메터/템플릿 검증을 "제외하고 진행"으로 변경
  · ALIAS 중복 → 마지막 행 사용 · 참조 불가/수식 오류 ADDP → 그 item만 제외
  · 제외된 ADDP를 참조하는 아래 ADDP도 연쇄 제외 · 템플릿도 해당 행만 건너뜀
  · 결과는 경고 목록으로 한 번에 안내(치명적 오류만 중단)
- plot: 정사각형 고정, 범례를 plot 바깥 오른쪽 위로
- 규격은 십자선 대신 **규격 창(박스)**을 빨간 실선으로, 타깃은 파란 X 표시

## v1.3.4
- 리포메터 읽기 오류 수정: ADDP FORM처럼 위쪽이 빈칸이고 아래에 수식이 오는
  열에서 polars 스키마 추론이 실패하던 문제("could not append value ... str").
  시트를 원시 값으로 받아 열 타입을 규칙으로 직접 결정(전부 숫자→Float64,
  그 외→문자열). 숫자 열이 문자열로 와도 SPECLOW/SCALE/order/page 파싱이 견딤

## v1.3.3
- 리포메터·Plot/Table 템플릿·실험 조건(xlsx)에 시트 선택 추가 — 시트가
  2개 이상이면 선택 창, 하나면 그대로. 모든 Excel 접근은 xlwings(data/xlio.py)
- 추출 프리셋에 리포메터 시트명 저장(reformatter_sheet)

## v1.3.2
- 실사용 모드가 기본 (데모는 `--demo` 플래그)
- DuckDB 신규 생성 지원 — 저장 다이얼로그에서 없는 파일명 입력
- 로그 축 item 패턴을 데이터 추출 화면 → 분석 도크(PLOT)로 이동
- DB 연결 로더: fact 스키마에서 item 자동 인식, 제외 목록 DB 영속·복원
- 그룹 미지정 시 '전체' 기본 그룹으로 표시
- [PPT 생성]이 실제 pptx 파일 저장 (실험 반복·표 분할 정책 포함)

## v1.3.1
- 개발 실행을 `python app.py` 로 (설치 불필요), 배포는 PyInstaller exe 유지
- UI 실제 구현: 데이터/분석 워크스페이스, 탐색·Summary·리포트 탭,
  실험 조건·그룹 편집 다이얼로그
- 화면과 PPT가 같은 matplotlib 렌더러를 쓰도록 통일
- DB·사내 패키지 없이도 데모 데이터로 전 화면 동작

## v1.3.0
- 자동 업데이트: 시작 시 사내 GitHub 릴리스 확인 → 선택 창
- 자릿수 규칙 확정: <1 → 3자리 · 1~10 → 2자리 · >10 → 1자리
- Summary avg/std 전역 토글 (동시 표시 없음)
