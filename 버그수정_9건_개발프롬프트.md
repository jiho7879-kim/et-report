# ET Report Tool — 분석 화면 버그 수정 9건 개발 프롬프트

> 이 문서는 코딩 에이전트(또는 세션)에 그대로 넘겨 줄 실행 프롬프트다.
> 항목별로 **원인 → 수정 → 금지사항 → 테스트**가 한 묶음이다. 순서대로 하되,
> 항목 1·9는 먼저 "이미 고쳐졌는지" 확인부터 한다(작업 트리에 미커밋 WIP가 있다).

---

## 0. 사전 컨텍스트 (반드시 읽고 시작)

**프로젝트**: ET Report Tool — PySide6 + polars + DuckDB + matplotlib + python-pptx.
문서·주석·UI 문자열은 전부 **한국어**. `CLAUDE.md`가 설계 규약이다(특히 "단일 진실
single source of truth" 표와 "계산은 명시적으로만").

**실행·검증**:
```bash
myenv/bin/python -m pytest                      # 기본 (slow 제외)
myenv/bin/python -m pytest -k "그룹 or table or ui" -q   # 관련 파일만
myenv/bin/ruff check .
```

**현재 트리 상태**:
- HEAD = `51c6639` (커밋됨)
- 작업 트리에 **다른 세션이 만든 미커밋 WIP**가 있다: `render/pptgen.py`(large),
  `model/specs.py`, `model/templates.py`, `export/excel.py`, `ui/tabs/explore.py`,
  `ui/tabs/summary.py`, `demo.py`, `CLAUDE.md`, `CHANGELOG.md` 등.
  **이 WIP를 되돌리거나 갈아엎지 말 것.**
- **항목 1·9의 원인이 되는 `table_mode_of`·`build_deck(tables=...)`는 HEAD에
  없고 WIP에 이미 추가되어 있다.** 즉 두 traceback은 HEAD/배포 exe 기준이다.
  → 항목 1·9는 "확인 → 회귀 테스트 추가"가 본질이고, 본 수정은 이미 돼 있을
  가능성이 높다.

**테스트 전제**: Excel(xlwings)·bdq 없이 전부 돌아야 한다. 모달 창
(`QMessageBox`, `QProgressDialog`)은 headless 테스트에서 영원히 멈추므로 반드시
`tests/test_ui_smoke.py`의 `no_modal_dialogs` 픽스처처럼 가로챈 뒤 테스트한다.

---

## 1. [적용] 시 ImportError — `cannot import name table_mode_of`

**증상** (`mainwindow._after_load` → `analysis_ws.connect_db` → `session.apply_config`):
```
from etreport.render.pptgen import table_mode_of
ImportError: cannot import name table_mode_of from etreport.render.pptgen
```

**원인**: HEAD의 `render/pptgen.py`에 `table_mode_of`가 없다. 작업 트리 WIP의
`render/pptgen.py:84`에 이미 정의되어 있다(미커밋).

**작업**:
1. 확인: `src/etreport/render/pptgen.py`에 `def table_mode_of(mode: str) -> str`가
   있는지, `src/etreport/model/session.py:137`의 import가 실제로 성공하는지
   (`myenv/bin/python -c "from etreport.render.pptgen import table_mode_of"`).
2. 없으면 정의를 추가한다(WIP 버전과 동일 의미):
   `"split"만 split, 그 외(wide 포함)는 overflow` — §10.8.
3. **회귀 테스트 추가**: `session.apply_config`가 리포메터/템플릿 없이도
   `table_mode_of(cfg.table_slide_mode)` 경로를 통과하는지. `test_review_fixes.py`
   또는 `test_ui_smoke.py`에:
   - `AnalysisConfig(table_slide_mode="wide")` → `state.table_slide_mode == "overflow"`
   - `"split"` → `"split"`

**금지**: `session.py`에서 `table_mode_of` import를 지우거나 try/except로 삼키지
말 것 — 'wide' 값 흡수는 확정 동작이다.

**배포**: 현장 exe가 옛 빌드면 재빌드가 필요하다. 코드 수정만으로 끝내지 말고
릴리스 노트에 "재빌드 필수"를 남긴다.

---

## 2. 온도 보정 — 5단위 정수값

**증상**: `temperature`가 23.9, 24.9, 149처럼 나온다. -40, 25, 45, 85, 125,
150 같은 5단위 정수가 되어야 한다.

**원인**: DB에는 raw 온도(FLOAT)가 저장되고, `data/compat.py`의
`select_sql()`·`wafer_index_sql()`·`ctx_select()`가 그대로 읽는다.

**수정 — "로딩 시점(read-time)에 보정"**:
기존 DB까지 보정되도록 **추출/적재 시점이 아니라 읽는 시점**에 반올림한다
(CLAUDE.md의 "ABSOLUTE는 로딩할 때 다시 건다"와 같은 철학).
- `compat.py`에 보정 헬퍼 추가, 예:
  ```python
  TEMP_STEP = 5
  def temp_expr(role_col: str) -> str:
      """온도를 5단위 정수로 — 23.9→25, 149→150. NULL은 그대로."""
      return f"CAST(ROUND(\"{role_col}\" / {TEMP_STEP}) AS BIGINT) * {TEMP_STEP}"
  ```
- 적용 위치(전부, 빠뜨리면 merge 키·필터 콤보·배정 비교가 어긋난다):
  1. `ctx_select()` — temp 열 (wide 경로 outer + long 경로)
  2. `select_sql()` **long 경로의 PIVOT 그룹 키** (base SELECT에서 temp에 적용)
  3. `select_sql()` merge 경로의 `merge_cols()` inner SELECT의 temp (같은 wafer라도
     raw 23.9와 25.0이 다른 측정점으로 갈라지지 않게 **병합 전에** 보정)
  4. `wafer_index_sql()` — 그룹 편집 조회(콤보가 보정된 값만 보이도록)
- `apply_manual_groups()`는 문자열 비교라서, 인덱스(보정됨)와 프레임(보정됨)이
  같은 표기(예: `"25"`)가 되어야 매칭된다. 보정 결과가 정수라
  기존 "25.0" 표기 문제도 함께 사라진다.
- `db.py`/`extractor.py`(적재)는 **손대지 않는다** — 보정 경로를 두 곳에 두지
  않는다(단일 진실). 단, 기존 DB가 이미 raw라도 읽기 시점 보정으로 화면·PPT가
  맞다.

**테스트** (`tests/test_step_seq_merge.py` 또는 새 `test_temp_round.py`):
- 임시 duckdb에 temp 23.9 / 24.9 / 149 / -40 / NULL을 넣고
  `compat.select_sql` 결과가 25 / 25 / 150 / -40 / NULL인지
- 같은 lot·wafer·step·site에서 temp 23.9와 25.0이 **한 측정점으로 병합**되는지
- `wafer_index_sql`이 보정된 temp만 반환하는지
- `apply_manual_groups` 키가 보정된 값(`"25"`)으로 매칭되는지

**주의**: ROUND의 반올림 규칙(절반은 0에서 멀어지는 쪽)을 테스트로 고정하고,
다른 컬럼(x·y·step·site)에는 적용하지 말 것.

---

## 3. [적용] 시 summary/Report 자동 그리기 금지 — 그리기는 버튼으로만

**증상**: 분석 탭에서 [적용]을 누르면 Summary 표와 Report 미리보기가 자동으로
그려진다. 각각의 그리기 버튼으로만 그리고 싶다.

**현재 구조**:
- `ui/analysis_ws.py:apply_config()` → `bus.data_changed.emit()` +
  `bus.report_changed.emit()`
- `ui/tabs/explore.py:101` — `data_changed`/`explore_changed` → `mark_stale()`
  (`auto_refresh=True` 기본값 → **보이면 즉시 refresh**)
- `ui/tabs/report.py:111` — `report_changed`/`groups_changed`/`data_changed` →
  `mark_stale()` (마찬가지로 보이면 즉시 rebuild)
- `ui/tabs/summary.py:41` — 이미 `auto_refresh = False` (dirty만, [표 만들기]로만)

**수정**:
1. `explore.py`·`report.py`의 자동 그림 정책을 **신호별로 나눈다** (결정
   Q3-2=B):
   - `data_changed`·`explore_changed`·`report_changed` → **dirty만** 남긴다.
     [적용] 직후에는 아무 탭도 자동으로 그리지 않는다. 그리기는
     [그리기]/[미리보기]/[표 만들기] 버튼으로만.
   - `groups_changed`(그룹 토글·그룹 편집) → **즉시 반영 유지**.
     구현: `explore._groups_changed`는 `mark_stale()` 대신 `refresh()`를,
     `report.py:111`의 `groups_changed` 연결은 `mark_stale` 대신 `refresh()`
     (또는 전용 핸들러)를 부르게 바꾼다. `summary.py`는 기존대로 dirty만.
2. **예외 유지**: 점 클릭 제외/복원(`_on_exclusion`)은 즉시 반영을 그대로 둔다
   (mark_stale을 우회하는 기존 경로를 건드리지 않는다).
3. `StaleMixin.showEvent`의 "탭을 켜면 stale일 때 refresh"는 유지한다(보고 있는
   탭은 그린다).
4. `analysis_ws.py`의 `apply_config()`에서 `data_changed`/`report_changed` emit은
   유지(다른 소비자·도크 갱신에 필요)하되, 이로 인한 탭 자동 그림은 위 정책으로
   막는다.

**테스트** (`test_ui_smoke.py`):
- `mark_stale` 호출 후 `refresh()`가 **호출되지 않는지** (auto_refresh=False 확인)
- [그리기]/[미리보기] 버튼 클릭 시에만 그려지는지 (스파이로 refresh 호출 수 검증)

---

## 4. 그룹 편집에서 DB 변경 시 기본 설정 재적용 (DB 파생 부분만)

**증상**: 그룹 편집에서 DB를 바꿔도 이전 DB 기준의 배정/인덱스가 남는다.
새 DB에 맞는 기본 상태로 갱신되면 좋겠다.

**현재 구조**: `ui/widgets/group_dialog.py:_pick_db()`는 `self.db_path`를 바꾸고
`_load_index()`로 wafer 인덱스만 다시 읽는다. `state.manual_groups`와
`state.groups`는 이전 DB 것을 그대로 품는다.

**수정** (`group_dialog.py:_pick_db`):
1. 고른 DB가 `state.db_path`와 **다른** DB면(새 DB로 전환):
   - `state.manual_groups`를 비운다 (이전 DB의 lot/wafer 키는 새 DB와 무관)
   - 자동그룹핑(항목 5)을 **새 DB 기준으로 다시 실행**한다 (결정 Q4-1=C —
     항목 5 모드 콤보의 현재 선택을 따른다. 아래 "기본 설정"의 의미 참조)
   - 그룹 **정의**(이름·색·심볼·REF)는 유지한다 — "db만" = DB에서 파생된
     인덱스·배정만 새 DB 기준으로
2. 같은 DB(또는 state.data가 그 DB)면 아무것도 초기화하지 않는다(기존 동작).

**"기본 설정"의 의미 (결정 Q4-1=C — 둘 다, 2026-08-13 사용자 확정)**:
- (1) **프리셋 DB 경로 따라가기**: `_edit_groups`가 항상 `self.cfg().db_path`를
  넘긴다 (이미 동작 중 — 유지 확인).
- (2) **자동 배정 재실행**: `_pick_db`에서 DB가 실제로 바뀌면(위 수정) 자동그룹핑
  (항목 5)을 새 DB 기준으로 다시 실행한다 — "켜져 있으면"이 아니라 **항상**
  재실행한다(DB가 바뀌는 순간이 곧 자동그룹핑 트리거).

**테스트** (`tests/test_group_dialog.py`):
- DB1으로 배정 → DB2로 교체 → manual_groups가 비워지고 인덱스가 DB2인지
- 같은 DB 재선택 시 아무것도 초기화되지 않는지

---

## 5. 자동그룹핑 모드 (wafer별 자동 배정)

**요구**: 그룹 편집에서 "자동그룹핑"을 하면 wafer별로 자동 배정된다.

**설계** (`ui/widgets/group_dialog.py` + `model/state.py`):
1. 그룹 편집 창에 [자동 그룹핑] 콤보 + 실행 버튼 추가. **모드는 사용자가
   선택한다** (결정 Q5-1=C):
   - **wafer별**: (lot, wafer)마다 그룹을 하나씩 만들고(이름은 wafer ID,
     결정 Q5-3=A), 팔레트 색을 순환 배정, 해당 wafer의 모든 측정점을 배정
   - **lot별**: lot마다 그룹
   - 자동 임계값 대체는 두지 않는다(모드로 직접 고르므로 — Q5-2 해당 없음).
4. 자동 배정 전에 기존 manual_groups는 지운다(재실행 멱등).
5. `state.groups`가 비어 있으면 생성한 그룹으로 채우고, 있으면 새 wafer 그룹을
   추가한다. 첫 그룹은 REF(기존 `_add_group` 규칙)로.

**테스트**:
- wafer별 자동 배정 → `manual_groups`에 (lot, wafer, None,None,None) 키가
  wafer 수만큼 생기는지
- `session.apply_config`로 재로딩한 뒤에도 `state.data`의 gid가 유지되는지
  (항목 6과 함께 회귀 검증)

---

## 6. [적용] 전에 짠 그룹이 [적용] 후 초기화되는 버그

**증상**: [적용] 전에 그룹 편집에서 단일 lot을 조회해 배정했는데, [적용]을
누르면 table과 plot에 배정이 반영되지 않고 초기화된다.

**원인 (확정)**:
- `ui/analysis_ws.py:apply_config()`는 `session.apply_config()` 뒤에
  ```python
  if self.state.split is not None and self.state.factors:
      self._apply_split(silent=True)
  ```
  를 또 호출한다.
- `_apply_split()`은 `st.groups = st.split.styles_for(st.factors)`로 **그룹 목록을
  교체**하고, `split.assignment()`로 **gid를 다시 지정**한다.
- 그런데 `data/loader.py:load_state()`는 이미 "split 배정 → manual_groups
  재적용(사용자가 고른 쪽이 이긴다)"을 올바른 순서로 끝냈다. 뒤이은 중복
  `_apply_split`이 그 결과(그룹 편집에서 추가한 그룹 + 손배정)를 덮어쓴다.
- 실험 조건 파일을 쓰는 사용자는 항상 이 경로를 타므로 "초기화된다"로 보인다.

**수정**:
1. `analysis_ws.py:apply_config()`에서 중복 `_apply_split(silent=True)` 호출을
   **제거**한다 — split 배정은 `load_state`가 이미 한다.
2. `_apply_split()`은 "사용자가 factor를 편집한 뒤" 경로(`_edit_split`)에서만
   남는다. 이 경로에서도 manual 배정이 이기도록, split 배정 적용 **뒤에**
   `loader.apply_manual_groups(st.data, st.manual_groups)`를 다시 건다
   (`load_state`와 같은 순서).
3. `st.groups` 교체도 manual로 추가한 그룹이 사라지지 않게 한다: split 스타일
   뒤에 manual_groups가 참조하는 그룹이 있으면 유지한다. (간단한 방법: 교체
   전에 기존 그룹과 gid가 겹치면 이름/스타일을 보존.)

**테스트** (회귀 — 먼저 실패하는 걸 확인하고 고칠 것):
- `test_group_dialog.py` 또는 `test_review_fixes.py`:
  - split 없이: manual_groups를 미리 설정 → `session.apply_config(state, cfg)` →
    `state.data`의 해당 wafer gid가 manual 값인지, `state.groups`가 유지되는지
  - split 있음: 위와 동일 + split 배정이 manual에 밀리는지 (CLAUDE.md의
    "사용자가 고른 쪽이 이긴다")

---

## 7. 병목 버튼에 로딩 화면

**요구**: 분석 탭에서 오래 걸리는 버튼 동작에 로딩 표시가 있으면 좋겠다.

**현재 구조**:
- `ui/widgets/worker.py:run_in_background()` — QProgressDialog(취소 없음,
  WindowModal) + QThread. PPT 생성·xlsx는 이미 사용 중.
- [적용]은 UI 스레드에서 동기 실행 + WaitCursor만 (`analysis_ws.py:311`).
- 탐색 [그리기]·리포트 [미리보기]는 UI 스레드에서 matplotlib 렌더(데이터가 크면
  수 초~수십 초).
- SQL 조회/내보내기(`ui/widgets/sql_dialog.py:_run/_save_*`)는 동기.

**수정**:
1. **[적용]**: `session.apply_config(state, c)`를 `run_in_background`로 넘긴다.
   - `LoadReport`가 돌아오면 **done 콜백에서만** UI를 갱신한다
     (`rep_combo` 채우기, `_applied`/dirty, `settings.save()`,
     `data_changed`/`report_changed` emit, `_refresh_dock`, 경고 QMessageBox).
   - 모달 진행창이므로 테스트는 `no_modal_dialogs` 픽스처로 가로챈다.
   - `connect_db()`(데이터 적재 직후 자동 연결)도 같은 경로를 탄다.
2. **[그리기]/[미리보기]**: matplotlib 렌더를 워커 스레드에서 수행하고 완료 후
   캔버스에 결과를 붙인다.
   - `render/mpl_renderer.py`의 렌더는 pyplot을 쓰지 않고 `Figure()`를 직접
     만든다 — 스레드에서 Figure를 새로 만들어 렌더한 뒤, done에서
     `plot_canvas.PlotCanvas`의 figure를 교체/재표시한다(화면=PPT 원칙 유지).
   - 도저히 스레드 렌더가 안 되는 부분이면 최소: 버튼 비활성 + "그리는 중…"
     텍스트 + WaitCursor (이 경우 이 계획에 그렇게 명시).
   - 클릭 제외(`_on_exclusion`) 즉시 반영 경로는 건드리지 않는다.
3. **SQL 조회/내보내기**: `sql_dialog._run/_save_*`를 `run_in_background`로
   (DuckDB 쿼리·파일 쓰기가 큰 DB에서 느리다).
4. **표 만들기**([표 만들기])는 집계가 이미 빠르므로 busy 표시(버튼 비활성 +
   문구)만.

**테스트**: `test_ui_smoke.py`에서 [적용] 경로가 no_modal_dialogs 가로채기로
통과하는지. 백그라운드 렌더는 렌더 결과가 화면=PPT와 같은지 기존
`test_ui_smoke.py`의 복사 일치 검증으로 확인.

---

## 8. 표(Summary)에 그룹 배정이 반영되지 않음

**증상**: plot은 그룹 배정이 적용되는데, 표에는 DB의 모든 lot/wafer가 다 나온다.

**원인 (확정)**: 표의 단일 진실인 `export/excel.py:build_table()`이
`state.wafer_columns()`(전체)를 헤더로 쓰고 gid 필터가 전혀 없다. plot은
visible 그룹의 gid만 그린다(`export/deckbuild.py:_plot_data`).

**수정 — 표도 plot과 같은 "visible 그룹" 기준으로**:
1. 공통 헬퍼(예: `model/state.py`에 추가 또는 `export/excel.py`에):
   ```python
   def visible_wafer_columns(state) -> list[tuple[str, list[str]]]:
       """visible 그룹에 속한 wafer만. 그룹이 없으면 전체(plot의 _ALL 폴백과 동일)."""
       if not state.groups:
           return state.wafer_columns()
       visible = {g.gid for g in state.groups if g.visible}
       # (lot, wafer) → 해당 wafer의 행 중 하나라도 visible 그룹이면 포함
       ...
   ```
2. `build_table()`은 이 헬퍼로 헤더를 만든다. `wafer_stats()`/`ref_values()`는
   (item, lot, wafer) 키 기반이라 헤더를 좁히면 값도 따라 좁혀지고, 제외는
   내부에서 처리하므로 그대로 둔다.
3. **화면 Summary 탭도 같은 헬퍼를 쓴다** — `ui/tabs/summary.py:rebuild()`는
   `build_table()`을 안 쓰고 자체 루프로 그리므로 여기도 헬퍼를 적용해야 화면과
   복사/xlsx/PPT가 갈라지지 않는다(화면=출력 원칙).
4. 그룹 표시/숨김 토글(도크 체크박스 → `visible=False`)이 표 열에도 즉시
   반영되도록 `build_table`은 호출 시점의 visible을 읽는다.

**테스트**:
- 그룹을 만들어 일부 wafer만 배정 → `build_table` 헤더에 배정된 wafer만 있는지
- 그룹이 없으면(전원 gid="") 기존처럼 전체가 나오는지 (회귀 방지)
- `visible=False` 그룹의 wafer는 표에서 빠지는지
- Summary 화면 표와 `to_tsv`가 같은 열인지 (기존 복사 일치 테스트 확장)

---

## 9. PPT 생성 시 TypeError — `build_deck() got an unexpected keyword argument 'tables'`

**증상** (`export/deckbuild.py:68`):
```
prs = pptgen.build_deck(report=..., experiments=..., ..., tables=_tables(state), ...)
TypeError: build_deck() got an unexpected keyword argument 'tables'
```

**원인**: HEAD의 `build_deck()` 시그니처에 `tables` 파라미터가 없다. 작업 트리
WIP의 `render/pptgen.py:89`에 이미 `tables: list[TableData]`가 추가되어 있다.

**작업**:
1. 확인: `render/pptgen.py`의 `build_deck` 시그니처에 `tables`가 있고, 본문이
   "plot 전부 → 표 전부 → 제외 이력" 순서(§7.2)를 지키는지.
2. 없으면 WIP 버전과 동일하게 추가: `tables: list[TableData]` 받아
   `table_mode_of(table_mode)`로 overflow/split 분기, 표 셀은 병합 먼저·값
   나중(§10.5).
3. **회귀 테스트**: `tests/test_pptgen_deck.py`에 "deckbuild.generate()가
   tables 인자로 통과하는지" — 표가 있는 Report로 `deckbuild.generate`를 직접
   호출해 TypeError가 없는지. (실제 slide 수·순서 검증은 기존 테스트가 이미 함.)

**배포**: 항목 1과 동일 — 현장 exe 재빌드 필요.

---

## 공통 요구사항 (모든 항목)

- **MUST DO**
  - 한국어 주석/문서/UI 문자열 유지.
  - 단일 진실을 지킨다: 집계는 `model/aggregate.py`, 표는
    `export/excel.py:build_table`, 그리기는 `render/mpl_renderer.py`, 축 범위는
    `render/ranges.py`, 덱 조립은 `render/pptgen.py:build_deck` — 새 코드가
    우회하지 말 것.
  - "계산은 명시적으로만" 규약 유지(자동 재계산 추가 금지 — 항목 3은 그것을
    강화하는 방향).
  - 변경 파일은 `ruff check` 통과 + 관련 테스트 전체 통과.
  - 회귀 테스트는 **먼저 실패를 확인하고** 수정한다.
- **MUST NOT DO**
  - `as any`/`@ts-ignore`류 무시, 빈 except.
  - Excel은 xlwings 전용(xlio 캐시 경유) — openpyxl 등으로 우회 금지.
  - `key_hash_expr()`·적재 버킷 로직·`apply_absolute`의 "스케일 재적용 금지"
    원칙 변경 금지.
  - 모달 창을 headless 테스트에 그대로 노출(no_modal_dialogs로 가로챌 것).
  - 다른 세션의 미커밋 WIP 되돌리기.
- **검증 절차**
  1. `myenv/bin/ruff check .`
  2. `myenv/bin/python -m pytest` (기본 전부)
  3. 관련 테스트 파일 개별 실행
  4. 가능하면 `myenv/bin/python app.py --demo`로 UI 스모크 확인

## 우선순위·순서 제안

1. 항목 1·9 (확인 + 회귀 테스트 — 코드 수정은 거의 없을 수 있음)
2. 항목 6 (그룹 초기화 — 가장 치명적) → 항목 5·4 (자동그룹핑, 같은 파일)
3. 항목 8 (표 그룹 반영 — build_table 헬퍼)
4. 항목 2 (온도 보정 — compat SQL)
5. 항목 3 (적용 시 자동 그림 금지)
6. 항목 7 (로딩 화면 — 마지막, 의존성 적음)

---

# 부록 A — 결정 사항 & 질문 목록 (실행 전에 전부 답을 채운다)

> 실행 에이전트는 **이 목록의 "결정"이 채워지지 않은 항목에 대해서는 질문하지
> 않고, 기본값("권장")으로 진행**한다. 모호해서 질문이 필요한 곳은 여기에
> 미리 다 적어 두었으므로, 실행 중 추가 질문을 최소화한다.
> 사용자가 답을 주면 각 질문의 "결정:" 줄을 갱신한다.
> 표기: **권장** = 기본값. 결정이 없으면 권장으로 진행.

---

## 항목 1 관련

- **Q1-1** 재빌드/릴리스 범위
  - 옵션: (A) 코드+테스트만, 재빌드는 별도 작업으로 남김 / (B) 릴리스까지 포함
  - **권장**: A — 현장 exe는 사용자가 릴리스 절차(`build_release.py`)로.
  - 결정: (미정)
- **Q1-2** 회귀 테스트 위치
  - 옵션: (A) `test_review_fixes.py` (기존 회귀 모음) / (B) `test_ui_smoke.py` /
    (C) 새 파일
  - **권장**: A — 업데이트 가드·연결 회귀가 이미 모여 있는 곳.
  - 결정: (미정)

---

## 항목 2 — 온도 보정 (결정이 가장 많음)

- **Q2-1** 보정 방식
  - 옵션:
    (A) **가장 가까운 5의 배수로 반올림** — 23.9→25, 24.9→25, 149→150.
    사용자가 제시한 예시와 정확히 일치.
    (B) 알려진 명목 온도 집합 {-40, 25, 45, 85, 125, 150} 중 가장 가까운 값.
    반올림보다 안전하지만 목록 밖 온도(예: 60)는 못 살린다. 목록 유지보수 필요.
    (C) 내림(floor) — 23.9→20. 값이 치우친다.
  - **권장**: A (예시와 일치, 목록 불필요)
  - 결정: **A** — 가장 가까운 5의 배수로 반올림 (2026-08-13 사용자 확정)
- **Q2-2** 적용 시점
  - 옵션: (A) **읽기 시점(SQL)만** — 기존 DB도 보정, 경로 하나 / (B) 적재
    시점만 — 새 DB만 보정 / (C) 둘 다
  - **권장**: A — CLAUDE.md "로딩할 때 다시 건다" 철학과 일치. 적재 시점에
    고치면 이미 적재된 DB는 계속 raw다.
  - 결정: (미정)
- **Q2-3** 출력 타입
  - 옵션: (A) **정수(BIGINT)** — 25, -40. "5단위의 정수값" 요구와 일치.
    `apply_manual_groups` 문자열 비교도 `"25"`로 깔끔해진다.
    (B) FLOAT 유지(25.0) — 기존 표기 유지.
  - **권장**: A
  - 결정: (미정)
- **Q2-4** 절반 경계(예: 22.5 → 20? 25?) 반올림 규칙
  - 옵션: (A) **DuckDB ROUND 기본(절반은 0에서 멀어짐)** — 22.5→25 /
    (B) 절반 내림
  - **권장**: A — 측정 노이즈가 절반 경계에 정확히 걸릴 일이 거의 없다. 테스트로
    규칙만 고정한다.
  - 결정: (미정)
- **Q2-5** 보정 끄기 옵션이 필요한가
  - 옵션: (A) **항상 적용(옵션 없음)** / (B) settings에 토글 추가
  - **권장**: A — 지금 요구는 "항상 5단위 정수"라 토글은 과설계.
  - 결정: (미정)
- **Q2-6** 이미 5단위 정수인 DB는 변화 없음(변환 멱등) — 확인 테스트만. (결정 불필요)

---

## 항목 3 — [적용] 시 자동 그림 금지

- **Q3-1** 탭 전환(showEvent) 시 자동 갱신
  - 옵션: (A) **유지** — 탭을 열면 stale이면 그린다 / (B) 버튼 전용 — 탭을
    열어도 안 그리고 [그리기]/[미리보기]만
  - **권장**: A — [적용] 직후의 자동 그림만 없애자는 요구에 부합. 탭을 켰는데
    빈 화면이면 "왜 안 그려졌지?" 혼란이 더 크다.
  - 결정: (미정)
- **Q3-2** 그룹 토글(도크 체크박스)·그룹 편집 직후 반영
  - `auto_refresh=False`면 `groups_changed`도 dirty만 남기고 [그리기]를 눌러야
    반영된다. 현재는 토글 즉시 다시 그린다.
  - 옵션: (A) **모두 버튼 전용**(일관성) / (B) `groups_changed`만 즉시 반영
    (StaleMixin에 신호별 분기 필요 — 코드 변경 커짐)
  - **권장**: A — "계산은 명시적으로만"과 일치. 단, 그룹 토글 직후 표시가 안
    바뀌면 불편할 수 있으니 B가 낫다면 말할 것.
  - 결정: **B** — `groups_changed`만 즉시 반영, `data_changed`(적용)는 버튼
    전용. StaleMixin에 신호별 분기 필요 (2026-08-13 사용자 확정)
- **Q3-3** 점 클릭 제외/복원 즉시 반영 유지 — 옵션 없음(확정 유지). (결정 불필요)
- **Q3-4** [적용] 직후 버튼 dirty(주황) + "변경됨" 안내 표시 — 기존 동작 유지.
  (결정 불필요)

---

## 항목 4 — 그룹 편집에서 DB 변경 시 기본 설정 재적용

- **Q4-1** "기본 설정"의 의미 (가장 중요한 해석 질문)
  - 옵션:
    (A) **자동그룹핑(항목 5) 결과를 새 DB 기준으로 재배정** — DB를 바꾸면
    wafer별 자동 배정이 다시 실행된다.
    (B) 설정 프리셋(기본 설정)의 DB 경로를 그룹 편집이 따라가기만 하면 됨 —
    수정은 `_edit_groups`가 항상 `self.cfg().db_path`를 넘기도록 하는 정도.
    (C) A와 B 둘 다
  - **권장**: A — 항목 5의 자동그룹핑과 묶어 "DB 변경 = 기본 상태로 초기화 +
    자동 배정" 흐름을 만든다. B만 원하면 말할 것.
  - 결정: **C** — 둘 다: (1) `_edit_groups`가 항상 `self.cfg().db_path`를
    넘기도록 유지(프리셋 DB 경로 따라감) + (2) DB가 실제로 바뀌면 자동그룹핑(항목
    5)을 새 DB 기준으로 재실행 (2026-08-13 사용자 확정)
- **Q4-2** `manual_groups` 초기화 범위
  - 옵션: (A) **전부 비움** — 키가 (lot, wafer, …) 튜플이라 DB 소속을 구분할
    수 없다 / (B) 새 DB에 존재하지 않는 키만 (비교 비용 + 복잡도)
  - **권장**: A
  - 결정: (미정)
- **Q4-3** 그룹 정의(이름·색·심볼·REF)는 유지 — "db만"의 뜻. 옵션 없음. (결정 불필요)
- **Q4-4** `state.data`가 다른 DB일 때 교체(현재 동작 유지: `_pick_db`에서
  `state.data = None`). (결정 불필요)

---

## 항목 5 — 자동그룹핑 모드

- **Q5-1** 배정 단위
  - 옵션: (A) **wafer별 (lot+wafer)** — wafer마다 그룹 1개 / (B) lot별 —
    wafer가 많을 때 그룹 수 절약 / (C) 사용자가 모드 콤보로 선택
  - **권장**: A (요구사항 그대로). C는 구현 부담이 커진다.
  - 결정: **C** — 사용자가 콤보로 wafer별/lot별 모드 선택 (2026-08-13 사용자
    확정)
- **Q5-2** 그룹 수 폭발 임계값 (이 값 초과 시 lot별로 대체)
  - 옵션: (A) **24장 초과 시 lot별** / (B) 30장 / (C) 임계값 없음(무조건 wafer별)
  - **권장**: A — 화면·PPT 표가 다루기 쉬운 규모.
  - 결정: 해당 없음 — 모드 선택(Q5-1=C)으로 대체 (2026-08-13 사용자 확정)
- **Q5-3** 자동 생성 그룹 이름
  - 옵션: (A) **wafer ID 그대로**(예: "W01") / (B) "lot_W01" / (C) "G1…Gn" 일련번호
  - **권장**: A — 단일 lot 작업이 기본 흐름이라 구분이 쉽다.
  - 결정: (미정)
- **Q5-4** REF 그룹 지정
  - 옵션: (A) **첫 wafer 그룹을 REF로** / (B) REF 없음 / (C) 미배정을 REF로
  - **권장**: A — 기존 `_add_group` 규칙(첫 그룹 REF)과 일치.
  - 결정: (미정)
- **Q5-5** 재실행(멱등): 자동그룹핑 재실행 시 기존 manual_groups를 지우고 다시
  배정 — 옵션 없음(멱등이 기본). (결정 불필요)
- **Q5-6** 버튼 위치
  - 옵션: (A) **그룹 편집 다이얼로그** / (B) 분석 도크 그룹 섹션 / (C) 둘 다
  - **권장**: A — 항목 4와 같은 파일에서 처리.
  - 결정: (미정)
- **Q5-7** [적용] 전(DB만 고른 상태)에도 동작 — `wafer_index_from_db`로 가능.
  옵션 없음(동작이 기본). (결정 불필요)

---

## 항목 6 — [적용] 전 그룹 초기화

- **Q6-1** split 없이도 재현되는지 **먼저 테스트로 확인** — 코드상으로는 split
  경로에서만 덮어써진다. split 없이도 재현되면 추가 원인 조사가 필요하다.
  (결정 불필요 — 검증 항목)
- **Q6-2** `_apply_split`(factor 편집 경로)에서 그룹 목록 교체 시 기존(manual로
  추가한) 그룹 보존 규칙
  - 옵션: (A) **gid가 겹치면 기존 그룹의 이름/스타일을 보존** / (B) split 스타일
    우선(기존 동작 유지)
  - **권장**: A — "손으로 만든 그룹이 사라지면 안 된다"는 항목 6의 본질.
  - 결정: (미정)
- **Q6-3** manual 배정 > split 배정 순서는 CLAUDE.md 확정 사항 — 옵션 없음.
  (결정 불필요)

---

## 항목 7 — 로딩 화면

- **Q7-1** [적용] 진행 중 도크 조작
  - 옵션: (A) **진행 중 도크 비활성화**(안전) / (B) 비모달로 두고 조작 허용
  - **권장**: A — 재진입·중복 [적용] 방지. 기존 `setEnabled(False)` 패턴 유지.
  - 결정: (미정)
- **Q7-2** 로딩 표시 형태
  - 옵션: (A) **무한 진행창**(QProgressDialog, 취소 없음, WindowModal —
    `run_in_background` 기존 패턴) / (B) 비모달 오버레이 / (C) 버튼 텍스트 +
    WaitCursor
  - **권장**: A — 기존 PPT/xlsx와 동일한 UX. 테스트는 `no_modal_dialogs`로
    가로챈다.
  - 결정: (미정)
- **Q7-3** 단계별 진행률 — `session.apply_config`는 단계 콜백이 없다(LoadReport에
  경과시간만). 진행률 대신 무한 스피너가 기본. (결정 불필요)
- **Q7-4** [그리기]/[미리보기] 처리
  - 옵션: (A) **워커 스레드에서 matplotlib 렌더 → 완료 후 캔버스 교체** (창
    멈춤 없음, 렌더러가 pyplot을 안 쓰므로 가능) / (B) 최소: 동기 유지 + 버튼
    비활성 + "그리는 중…" + WaitCursor
  - **권장**: A — "로딩 화면" 요구의 핵심. 구현 난도가 있으면 B로 내리고
    계획에 그렇게 명시한다.
  - 결정: **A** — 워커 스레드 렌더 + 캔버스 교체 (2026-08-13 사용자 확정)
- **Q7-5** 스레드 렌더 후에도 클릭 제외(pick) 연결 유지 — 캔버스 교체 시
  `on_pick` 재연결 필수. (결정 불필요 — 구현 요구)
- **Q7-6** SQL 조회/내보내기(`sql_dialog._run/_save_*`) 백그라운드화 포함 여부
  - 옵션: (A) **포함** / (B) 제외(이번 범위 밖)
  - **권장**: A — 큰 DB에서 쿼리·파일 쓰기가 몇 초~수십 초.
  - 결정: (미정)
- **Q7-7** [표 만들기]는 집계가 이미 빠름 — busy 표시(버튼 비활성+문구)만.
  (결정 불필요)

---

## 항목 8 — 표에 그룹 반영

- **Q8-1** 그룹이 있을 때 **미배정(gid="") wafer** 표시
  - 옵션: (A) **숨김** — plot과 동일(visible 그룹만) / (B) "미배정" 구간으로
    표시
  - **권장**: A — plot과 일치하는 게 요구("plot은 적용됨"이 기준).
  - 결정: **A** — visible 그룹에 속한 wafer만 표시, 미배정은 숨김 (2026-08-13
    사용자 확정)
- **Q8-2** 한 wafer가 측정 조건(step/temp/site)별로 서로 다른 그룹에 걸린 경우
  - 옵션: (A) **하나라도 visible 그룹이면 wafer 열 포함** / (B) 전부 visible일
    때만 포함
  - **권장**: A — 표는 (lot, wafer) 단위라 부분 포함이 현실적.
  - 결정: (미정)
- **Q8-3** 그룹이 아예 없으면 전체 표시(현행 유지 — plot의 `_ALL` 폴백과 일치).
  (결정 불필요)
- **Q8-4** visible 토글(도크 체크박스)이 표에 즉시 반영. (결정 불필요 — 항목 3의
  Q3-2 결과와 함께 동작)
- **Q8-5** Summary 화면(자체 루프)도 같은 헬퍼 사용 — 화면=출력 원칙. (결정
  불필요)

---

## 공통

- **QG-1** 커밋 정책
  - 옵션: (A) **이번 작업은 커밋하지 않고 작업 트리에 남김**(다른 세션 WIP와
    섞이지 않게) / (B) 항목별로 커밋 / (C) 사용자가 직접 커밋
  - **권장**: A — 현재 트리에 다른 세션 WIP가 많아 섞이면 되돌리기 어렵다.
  - 결정: (미정)
- **QG-2** 사내 PC(Excel/xlwings) 검증 — 리눅스에서는 Excel 경로가 실행되지
  않는다. (A) 코드 리뷰 + headless 테스트로 갈음, 사내 확인은 사용자 / (B) 사내
  PC 접근 가능 시 직접 확인. **권장**: A. (결정 불필요 — 환경 제약)
- **QG-3** `--demo`로 눈 확인 대상 — 항목 3·4·5·7·8의 UI 변경은 데모 실행으로
  확인한다. (결정 불필요)

---

## 결정 요약표 (실행 전에 채운다)

| # | 질문 | 선택 | 비고 |
|---|---|---|---|
| Q1-1 | 재빌드 범위 | A (권장) | |
| Q1-2 | 회귀 테스트 위치 | A (권장) | |
| Q2-1 | 온도 보정 방식 | A (확정) | 5의 배수 반올림 |
| Q2-2 | 보정 적용 시점 | A (권장) | |
| Q2-3 | 보정 출력 타입 | A (권장) | |
| Q2-4 | 절반 경계 규칙 | A (권장) | |
| Q2-5 | 보정 토글 | A (권장) | |
| Q3-1 | 탭 전환 시 자동 갱신 | A (권장) | |
| Q3-2 | 그룹 토글 즉시 반영 | B (확정) | groups_changed만 즉시, 적용은 버튼 전용 |
| Q4-1 | "기본 설정" 의미 | C (확정) | 프리셋 DB 경로 따라감 + 자동그룹핑 재실행 |
| Q4-2 | manual_groups 초기화 | A (권장) | |
| Q5-1 | 자동그룹핑 단위 | C (확정) | 사용자 콤보 선택 (wafer별/lot별) |
| Q5-2 | 그룹 수 임계값 | 해당 없음 | 모드 선택으로 대체 |
| Q5-3 | 그룹 이름 형식 | A (권장) | |
| Q5-4 | REF 지정 | A (권장) | |
| Q5-6 | 버튼 위치 | A (권장) | |
| Q6-2 | 그룹 보존 규칙 | A (권장) | |
| Q7-1 | 적용 중 도크 비활성 | A (권장) | |
| Q7-2 | 로딩 표시 형태 | A (권장) | |
| Q7-4 | 그리기 렌더 방식 | A (확정) | 워커 스레드 렌더 + 캔버스 교체 |
| Q7-6 | SQL 백그라운드화 | A (권장) | |
| Q8-1 | 미배정 wafer 표시 | A (확정) | visible 그룹만, 미배정 숨김 |
| Q8-2 | 부분 포함 규칙 | A (권장) | |
| QG-1 | 커밋 정책 | A (권장) | |
