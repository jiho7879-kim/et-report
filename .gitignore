# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
myenv/

# 빌드 산출물
/*.spec                 # PyInstaller가 루트에 자동 생성하는 것
!build/ETReport.spec    # 빌드 정의는 저장소에 둔다
build/pyi/              # PyInstaller 작업 폴더
build/site_extras.py    # 사내 인증서·라이브러리 경로 — 커밋 금지
src/etreport/assets/build_info.json   # 빌드할 때 굽는 스탬프
dist/
*.zip
*.pptx

# 로컬 에이전트 설정 / 캐시
.claude/
.codegraph/
.pytest_cache/

# Windows ADS 잔재 (WSL에서 복사 시 생성)
*:Zone.Identifier

# 사내 데이터 — 절대 커밋 금지
*.duckdb
*.parquet
*.xlsx
*.xls
*.sbdf
settings.json
column_catalog.json
staging/
sessions/

# opencode 세션 아티팩트
.omo/
