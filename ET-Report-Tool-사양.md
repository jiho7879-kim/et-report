# ET Report Tool — 재구현 사양서

> 사내 ET(전기특성) 계측 데이터를 추출·가공해 탐색하고, 요약표와 PPT 리포트를
> 자동 생성하는 Windows 데스크톱 툴. 이 문서 하나로 처음부터 구현할 수 있도록
> 확정된 사양과 이미 발견한 함정을 모두 담았습니다.

---

## 0. 이 문서 사용법

- **§1~§3**은 반드시 먼저 읽어주세요. 환경 제약과 데이터 흐름이 설계를 결정합니다.
- **§10 "이미 겪은 함정"**은 실제로 터진 버그와 원인입니다. 같은 실수를 반복하지
  않도록 구현 전에 한 번 훑어주세요.
- 요구사항 중 **[확정]**은 논의를 거쳐 결정된 사항이니 임의로 바꾸지 마시고,
  **[판단]**은 구현하면서 더 나은 방법이 있으면 제안해주세요.

---

## 1. 환경 제약 — 설계를 좌우하는 조건

| 제약 | 내용 | 설계에 미치는 영향 |
|---|---|---|
| Excel 접근 | 사내 보안상 **xlwings만** 허용 (openpyxl·pandas.read_excel 불가) | COM 호출이 비싸므로 캐시 계층 필수 |
| 데이터 조회 | 사내 패키지 `bigdataquery`(bdq) 사용. `bdq.getColumnInfo(table)`, `bdq.getData(sql)` | bdq는 스레드 안전 확인됨 → 병렬 추출 가능 |
| API 제한 | `item_id IN (...)` 절에 **10,000개 초과 불가** | item 목록 분할 후 여러 쿼리로 나눠 실행 |
| 배포 | 사내 GitHub Enterprise에 릴리스, PyInstaller exe | 실행 중 exe는 잠기므로 zip 폴더 교체 방식 |
| 개발 실행 | `python app.py` (설치 없이) | 루트에 진입점 스크립트를 둘 것 |

**기술 스택**: Python 3.11+, PySide6, polars, DuckDB, pyarrow, matplotlib,
python-pptx, requests, packaging + (사내) bigdataquery, xlwings

---

## 2. 전체 데이터 흐름

```
[데이터 탭]
  리포메터 읽기 (xlwings, 1회)
      ↓  CATEGORY=REAL 인 ITEMID 목록 추출
  Impala 추출 (bdq.getData)   ← item_id IN (...) 로 반드시 좁힘
      ↓  기간 × item그룹 단위 청크, 4워커 병렬
  long parquet (고정 스키마)
      ↓  리포메팅: 스케일 → 절대값 → ALIAS 개명 → ADDP 계산
  DuckDB 적재 (테이블명 et_data, wide 구조)

[분석 탭]
  DuckDB 읽기 전용 조회
      ↓  step_seq 병합 → ABSOLUTE 재적용
  탐색(산점도) / Summary(표) / 리포트 구성(PPT)
```

**핵심 원칙 세 가지**

1. **화면 = 출력**: 탐색 화면, Summary 표, xlsx, PPT가 모두 같은 집계 함수와
   같은 렌더러를 씁니다. 따로 구현하면 숫자가 어긋납니다.
2. **읽고 계산하는 시점을 사용자가 정한다**: 파일을 고르는 동안에는 아무것도
   읽지 않고, [적용]·[표 만들기]·[그리기] 버튼을 눌렀을 때만 동작합니다.
3. **원본 DB는 절대 수정하지 않는다**: 분석은 읽기 전용 연결만 씁니다.

---

## 3. 파일 4종 스펙

사용자가 준비하는 엑셀 파일들입니다. 서로를 이렇게 참조합니다:

```
리포메터 (ITEMID → ALIAS 정의, 단위·규격·계산식)
   ↑ ALIAS로 참조
plot 템플릿 ─┐
             ├─ Report 컬럼으로 짝을 이룸
table 템플릿 ─┘
실험 조건 (lot·wafer별 step 조건 코드, 선택)
```

### 3.1 리포메터

컬럼: `CATEGORY, ITEMID, ALIAS, ABSOLUTE, SCALE FACTOR, ADDP FORM, UNIT, SPECLOW, SPECHIGH, TARGET`

- `CATEGORY`: `REAL`(실측) / `ADDP`(계산)
- `ITEMID`: DB의 실제 item_id. **REAL만 조회 대상** (ADDP는 계산 결과라 쿼리하지 않음)
- `ALIAS`: 툴 내부 이름. 템플릿의 x/y·item_id가 이 값을 가리킴
- `ABSOLUTE`: 절대값 적용 여부. **[확정] `TRUE/True/true/T/Y/1/O` = 참,
  `FALSE/N/0/X/빈칸` = 거짓. 대소문자 무관, 빈칸은 거짓**
- `SCALE FACTOR`: 원시값에 곱할 배율. **수식 계산 전에 적용**
- `ADDP FORM`: 계산식. `{ALIAS}`로 참조
  - 함수: `ABS SQRT LN LOG LOG10 EXP MIN MAX AVG SUM STD` (대소문자 무관)
  - **[확정] LN = 자연로그(밑 e), LOG·LOG10 = 상용로그** (엑셀 관례)
  - `Std(A,B,…)` = 인자들을 각각 한 점으로 보는 표본표준편차(n−1), NULL 제외
  - 참조 대상은 **위쪽 행**에 있어야 함 (행 순서 = 계산 순서, 순환참조 차단)
- `SPECLOW/SPECHIGH`: 표의 규격 이탈 표시 + plot 규격 박스 + 축 범위 산정
- `TARGET`: plot에 파란 X로 표시

**[확정] 검증 정책 — 문제 행은 버리고 진행**
- ALIAS 중복 → **마지막 행** 사용 (엑셀에서 아래에 덮어쓰는 습관과 일치)
- 참조 불가·수식 오류 ADDP → 그 item만 제외
- **제외된 ADDP를 참조하는 아래 ADDP도 연쇄 제외** (NULL이 조용히 퍼지는 것 방지)
- 제외된 항목은 목록으로 한 번에 안내. 단 **컬럼 누락 같은 구조적 오류는 중단**

**시트 선택 필수**: 한 파일에 공정별·버전별 시트를 여러 개 두므로, 시트를
바꿔 끼울 수 있는 전용 창이 필요합니다. 시트가 하나면 묻지 않습니다.

### 3.2 plot 템플릿

컬럼: `page, x, y, order, title1, title2, Report, Type, x_name, y_name`

- `page`: 페이지 번호. 같은 번호끼리 한 슬라이드
- `order`: 페이지 안 위치 **1~6, 왼쪽에서 오른쪽** (1·2·3 윗줄 / 4·5·6 아랫줄)
- `x`, `y`: 리포메터 ALIAS. **[확정] 쉼표로 여러 개 → 순서대로 xy쌍이 되어
  한 그림에 겹침** (`x: A, B` + `y: C, D` → (A,C)(B,D)). 그룹 스타일은 공유
- `title1`: 페이지 제목 (같은 page의 첫 행에만)
- `title2`: 이 plot의 제목
- `Report`: 리포트 이름. 한 파일에 여러 리포트를 담고 UI에서 선택
- `Type`: `scatter`. **[확정] `table`은 더 이상 쓰지 않음** (§7 참조)
- `x_name`, `y_name`: 축 표시 이름. 비우면 `ALIAS + 단위` 자동

**템플릿에 두지 않는 것**: 로그 축 여부와 축 범위. 로그는 item 이름 패턴
(`Ioff*`, `*Leak*`, `Jg*` — 설정에서 관리), 범위는 자동 규칙(§6). 대신 화면에서
plot마다 개별 지정할 수 있어야 합니다.

### 3.3 table 템플릿

컬럼: `item_id, CAT1, CAT2, CAT3, …, Report`

- `item_id`: **리포메터의 ALIAS와 같은 값** (ITEMID 아님)
- `CAT1`: 표를 나누는 기준. CAT1 하나 = 표 하나 = 시트 하나 = PPT 한 장
- `CAT2` 이후: 표 안의 계층. **[확정] CAT4, CAT5… 원하는 만큼 늘릴 수 있어야 함.
  개수 고정 금지** — 정규식으로 `CAT\d+`를 찾아 번호순 정렬
- 행 순서 = 표시 순서
- **[확정] 병합 규칙: 상위 CAT이 바뀌면 하위 병합도 끊는다** (값이 같아도
  다른 그룹이므로)

**템플릿에 두지 않는 것**: 평균/산포 선택(화면 토글), 자릿수(자동), wafer 열
(분석 대상에서 자동 결정)

### 3.4 실험 조건 (split)

lot·wafer별 step 조건 코드. 두 형식 모두 인식:
- wide: `lot | wafer | M1 | M5 | …` (열 이름이 곧 step 이름)
- long: `lot | wafer | step_id | code`

**[확정] 입력 경로 3가지를 한 창에서 선택**: 엑셀 파일 / CSV·TSV /
**클립보드 붙여넣기**. 붙여넣으면 즉시 파싱해 미리보기와 step 목록 표시.
(엑셀이 편하지만 Excel 없는 PC도 있고, 붙여넣기가 더 빠를 때도 많음)

---

## 4. 데이터 추출 (데이터 탭)

### 4.1 조회 조건

- `line_id` **필수** (파티션 컬럼 — 없으면 풀스캔)
- 기간: `tkout_time` 기준. **[확정] SQL에는 반드시 문자열 리터럴로 전달**
  (`tkout_time >= '2026-08-04 00:00:00'`)
- **[확정] `item_id IN (...)` 필수** — 리포메터의 REAL ITEMID만. 전체 item을
  긁으면 `bdq.getData`가 매우 느립니다
- 조건은 컬럼 타입에 따라 다르게 해석 (카탈로그에서 타입 조회):
  - 문자열: 띄어쓰기=여러 값, `!`=제외, `*`=LIKE, `"…"`=공백 포함, 정규식 모드
  - 숫자: `>=25`, `25~85`, `25 85 125`, `!0`
  - TIMESTAMP: `2026-08-01 ~ 2026-08-10`
- **NOT IN은 NULL 함정이 있으므로 항상 `OR col IS NULL`을 붙일 것**

### 4.2 청크 분할

**[확정] 청크 = 기간 × item 그룹의 곱**

item이 10,000개를 넘으면 그룹으로 나눠 각각 쿼리합니다. 예: item 24,180개 →
3그룹 × 기간 7일 = 21청크, 4워커 병렬. parquet 파일명에 그룹 번호를 넣어
같은 날짜끼리 덮어쓰지 않게 합니다.

### 4.3 진행 표시 [확정]

어느 단계에서 시간이 가는지 보여야 합니다. 화면 하단에 고정된 로그 패널 +
진행률 바 + 단계명:

```
09:14:02  리포메터 열기: reformatter_M2.xlsx [M2]
09:14:03    REAL 48 · ADDP 12  (2.4초)
09:14:03    ⚠ 31행 Vt spread: 참조 불가 P040_X — 이 item을 제외했습니다
09:14:03  조회 item 24,180개 (REAL만, ADDP 제외) — 상한 초과로 3개 그룹 분할
09:14:03  추출 시작 — 2026-08-04 ~ 2026-08-10 · 청크 21개 · 워커 4
09:14:19    청크 5/21 완료  (08-04 item 2/3)
09:15:02  추출 완료 — 1,284,003행 (long) · 46.2초
09:15:02  리포메팅 시작 — 파일 7개 · ADDP 12개
09:15:03    파일 1/7  183,429행 → 219,115행  (1.1초)
09:15:10  적재 완료 — 254,110행 · 11.8초
09:15:10  ── 전체 68.1초 ──
```

로그 복사 버튼, 중지 버튼 포함. 진행률 바에는 `리포메팅 3/7 · ADDP Vt spread`
처럼 현재 계산 중인 item까지 표시.

### 4.4 DuckDB 적재

- **[확정] 테이블명 `et_data`** — 사용자가 손으로 만들어 쓰던 DB와 동일해야
  `select * from et_data`가 그대로 동작합니다. 예전 버전이 만든 `fact`도 인식
- wide 구조 (키 컬럼 + item 컬럼들), `INSERT BY NAME`으로 스키마 진화
- `key_hash` 기준 ANTI JOIN으로 중복 차단, 파일 단위 적재 이력 테이블
- 피벗은 key_hash 버킷(512)으로 나눠 메모리 피크 제어

---

## 5. 분석 탭 구조

### 5.1 왼쪽 도크 (설정 패널)

```
설정        [프리셋 콤보] [저장] [새 이름] [삭제]
            DB          et_data.duckdb  [et_data]
            Plot        plot_tpl.xlsx  [Sheet1]
            Table       table_tpl.xlsx [Sheet1]
            리포메터     reformatter.xlsx [M2]
            실험 조건    split_M2.xlsx
            [ 적용 ]                    ← 검증은 여기서 한 번만
            적용됨 · 2.4초 · 제외 3건
            [SQL 조회 · 내보내기]
            [Excel 캐시  12개 · 340 KB]
PLOT        로그 축 item 패턴: Ioff*, *Leak*, Jg*
실험 조건    [factor 편집]  factor · M1
            ⚠ 혼입 3건 — 'Split_A' 안에 M5가 2종 섞여 있습니다
그룹        ☑ Split_A  48    ☑ Base · REF  48
            [그룹 편집]
제외 포인트   12
            [되돌리기]
```

**[확정] 파일 선택 ≠ 읽기.** 파일을 고를 때는 경로만 담아 두고, [적용]을
누를 때 리포메터 → 템플릿 → 실험조건 → DB 순서로 한 번에 읽고 검증합니다.
파일마다 재검증하면 xlwings 때문에 매우 느립니다.

**[확정] REPORT 콤보는 두지 않습니다.** 템플릿에 리포트가 여럿이면 적용 후
안내 문구로만 표시합니다.

**설정 프리셋 저장**: DB·템플릿 2종·시트명·리포메터·로그축 패턴·실험조건 경로를
한 묶음으로 이름 붙여 저장/전환/삭제.

### 5.2 탐색 탭

- 왼쪽: 산점도 캔버스. **점을 클릭하면 제외/복원**
- 상단: [클릭 → 제외/복원] 모드, [＋ 리포트에 추가], **[그리기]** 버튼
- 오른쪽 패널 세 카드:
  1. **축** — X/Y 입력(자동완성), REF μ±3σ 밴드 체크
  2. **스케일 · 범위** — X/Y 각각 자동·log·선형, 범위 직접 지정
     (켜면 현재 축 값을 자동으로 채워줄 것)
  3. **그룹 스타일** — 그룹 선택 → 색·심볼·크기·REF 지정

**[확정] 자동완성은 로컬 문자열 매칭만.** AI 호출 금지. 쉼표 뒤 마지막
토큰만 완성합니다.

**동기화 주의**: 그룹 스타일은 도크·그룹편집 다이얼로그·탐색 패널 세 곳에서
바뀔 수 있습니다. 시그널 한 방향(`groups_changed`)으로 모으고, 컨트롤을
갱신할 때는 시그널을 막아 재귀를 피하세요.

### 5.3 Summary 탭

- 상단: **[표 만들기]** 버튼, 평균/산포 선택, Δ vs REF 체크, [전체 xlsx 내보내기]
- CAT1마다 카드 하나. 각 카드에 [복사](TSV) [xlsx] 버튼
- 열 구성: `CAT2 … CATn | item | (lot 헤더 병합) wafer들`
- 규격 이탈 셀은 붉은 배경
- 제외된 점이 있으면 `−N` 표기

### 5.4 리포트 구성 탭

- 왼쪽: 페이지 레일
- 가운데: 16:9 슬라이드 미리보기, **2×3 슬롯에 실제 미니 플롯**
- **[확정] 슬롯을 드래그&드롭으로 재배치** (order가 곧 위치이므로)
- 슬롯 클릭 → 오른쪽 인스펙터에서 제목·X·Y·로그축 편집, 비우기
- **[확정] 이 탭에서도 점 클릭 제외 가능.** "모든 plot에서 함께 제외" 옵션
  (기본 켜짐 — 표·PPT까지 일관되게 빠지는 쪽이 안전)
- **[확정] [템플릿에 저장]** — 화면 배치를 plot 템플릿 엑셀에 되씀.
  다른 Report 행은 보존하고 원본은 `.bak`으로 백업
- [PPT 생성] 버튼

---

## 6. 렌더링 규칙 [확정]

화면과 PPT가 **같은 matplotlib 렌더러**를 씁니다. 따로 두면 "화면=PPT"를 매번
대조해야 합니다. 축 범위를 규칙으로 고정하므로 줌·팬은 필요 없습니다.

| 항목 | 규칙 |
|---|---|
| plot 모양 | **항상 정사각형** (`set_box_aspect(1)`) |
| 범례 | **plot 바깥 오른쪽 위** |
| 축 범위 | `lo = min(SPECLOW, 데이터 min)`, `hi = max(SPECHIGH, 데이터 max)` → 중심 기준 폭 ×1.2 |
| 로그 축 | item 이름 패턴으로 자동 판정. plot별 오버라이드 가능 |
| 멀티 xy쌍 | 축의 모든 item 규격을 **합집합**으로 |
| 규격 표시 | **빨간 실선 박스**. 십자로 삐져나온 선이 아니라 규격 창. 한쪽 규격이 없으면 그 변은 축 끝까지 열어 둠 |
| 타깃 | **파란 X**. 양축 모두 있으면 교점 하나, 한쪽만 있으면 그 축의 선 |
| REF 밴드 | 회색 μ±3σ 영역 + 평균선 |
| 제외된 점 | 사라지지 않고 **회색 빈 원**으로 남김 |
| 배경 | 흰색 (PPT에 그대로 들어감) |
| 폰트 | 한글 깨짐 방지 — Malgun Gothic 우선 탐색 |
| 자릿수 | \|값\|<1 → 3자리, 1~10 → 2자리, >10 → 1자리 |

---

## 7. PPT 출력 [확정]

### 7.1 슬라이드 크기 — 항상 16:9

**13.33 × 7.5 인치 고정.** pptx는 슬라이드 크기가 프레젠테이션 전역이라,
표 때문에 덱을 넓히면 plot 페이지까지 가로로 늘어집니다. 절대 바꾸지 마세요.

### 7.2 페이지 순서

```
1) plot 페이지 — 모든 실험을 앞쪽에 쭉
2) 표 페이지  — 뒤쪽에 한 벌만
3) 제외 포인트 이력
```

**표는 (lot, wafer)별 집계라 실험 구분과 무관합니다.** 실험마다 다시 붙이면
같은 표가 중복되므로 한 벌만 만듭니다.

### 7.3 표 페이지

- **plot 템플릿과 무관하게 전용 페이지로 생성.** 템플릿에 `Type=table` 행을
  둘 필요가 없습니다
- CAT1마다 한 장
- wafer가 많을 때 두 가지 모드 (리포트 구성 탭에서 선택):
  - **overflow (기본)** — 글자 9pt를 지키고 **표가 슬라이드 오른쪽 경계를
    넘어가게 둡니다.** 발표 화면 밖으로 나가지만 편집 화면·인쇄에서는 온전합니다.
    제목에 안내 문구 표시
  - **split** — wafer 12장씩 나눠 여러 장으로. 제목에 `(1/3)` 표기
- **글자를 9pt 아래로 줄이지 않습니다.** 숫자가 뭉개지는 것보다 넘치는 게 낫습니다

### 7.4 표 스타일

화면(style.qss)과 같은 톤. 기본 파란 줄무늬 스타일 제거, 회색 헤더,
교대 행 배경, 얇은 가로선, 헤더 아래만 진한 선, 값 열 우측 정렬,
규격 이탈 셀만 붉게.

---

## 8. 실험 조건 기반 자동 분석 [확정]

이 툴의 핵심 기능입니다.

1. **factor 선택** — 비교할 step을 고르면 조건 코드 조합별로 그룹 자동 생성.
   `Base` 조합은 자동으로 REF(회색)
2. **혼입(confound) 감지** — 선택한 factor로 나눈 그룹 안에서 다른 step 코드가
   2종 이상이면 경고:
   > ⚠ 'Split_A' 그룹 안에 M5가 2종 섞여 있습니다 (Base, Hi)
   > 이 상태로는 차이가 M1 때문인지 M5 때문인지 구분할 수 없습니다.

   **자동 그룹핑은 편한 만큼 틀린 결론을 빠르게 만들어냅니다. 이 경고가
   기능의 핵심입니다.**
3. **실험별 페이지 반복** — 템플릿 N페이지 × factor M개 = N×M 페이지.
   실험마다 그룹만 바뀌고 plot 구성은 동일. 페이지 제목에 실험명 자동 부착

---

## 9. 그룹 편집 다이얼로그 [확정]

### 9.1 단일 lot 탭 — 4단 연쇄 필터

```
DB    et_data.duckdb · 불러옴                      [DB 선택]
lot   [PA100        ]  [조회]      유효 3장 · 12포인트
step_id [M5 ▾]   site [9 ▾]   temp [85.0 ▾]

미배정 wafer          →  ←  ≫  ≪        [그룹 콤보] [＋][이름][－]
  W01                                      W02
  W03
```

- **[확정] lot → step_id → total_site_cnt → temperature 순으로 좁힙니다.**
  앞을 바꾸면 뒤 콤보 목록과 유효 wafer가 다시 채워집니다
- **[확정] 배정도 필터 범위에만 적용.** 같은 wafer라도 step·온도가 다르면
  다른 측정점이므로
- **[확정] [조회] 버튼은 [적용] 없이도 동작해야 합니다.** DB 선택란에 경로가
  있으면 그 자리에서 읽기 전용으로 열어 조회합니다. 대부분의 사용자가
  "DB만 고르고 그룹부터 짜는" 흐름을 씁니다
- lot 찾기는 3단계로 넓혀갑니다: 정확히 일치 → 대소문자 무시 → DB에 LIKE 조회.
  못 찾으면 비슷한 lot을 예시로 제시
- 그룹 ＋추가 / 이름 변경 / －삭제 (실험 조건 파일 없이도 수동 그룹핑 가능)

### 9.2 멀티 lot 탭

엑셀에서 `lot · wafer · group` 표를 복사해 붙여넣기.

### 9.3 스타일 일괄

심볼·크기 전체 적용, 팔레트 재배정 (Okabe-Ito 색약 안전 / System / 그레이스케일).
REF는 회색 유지.

---

## 10. 이미 겪은 함정 — 반드시 피해주세요

실제로 터졌던 버그와 원인입니다.

### 10.1 step_seq 때문에 plot이 비는 문제 ★가장 중요

x는 `step_seq=1`, y는 `step_seq=2`에 기록되는 경우가 흔합니다. seq를 키에
넣으면 **두 행으로 갈려 x·y가 함께 있는 행이 0개**가 되고 그림이 비어버립니다.

세 가지를 모두 처리해야 합니다:
1. 조회 SQL이 키 컬럼(`step_id·total_site_cnt·temperature·step_seq·die 좌표`)을
   표준 이름으로 **함께 SELECT** 해야 합니다 (병합에도, 조회 조건 콤보에도 필요)
2. retest 중복 제거(QUALIFY) 파티션에는 **step_seq를 포함**해야 합니다.
   빼면 정상 행까지 하나만 남고 나머지가 사라집니다
3. 로드 후 **step_seq만 다른 행을 한 측정점으로 병합**합니다.
   키는 `(lot, wafer, die_x, die_y, temperature, step_id, site_cnt)`,
   각 item은 NULL이 아닌 첫 값. **step_id·온도·site_cnt가 다르면 합치지 않습니다**

### 10.2 ABSOLUTE가 안 먹는 것처럼 보이는 문제

ABSOLUTE를 **추출 시점에만** 적용하면, 이미 DB에 음수로 적재된 값은 리포메터를
고쳐도 그대로입니다. **분석 로딩·[적용] 시점에도 절대값을 적용**하세요.
절대값은 멱등(`|x|` 두 번 = `|x|`)이라 안전합니다. 단 **스케일은 멱등이 아니므로
절대 재적용하면 안 됩니다.**

### 10.3 polars 스키마 추론 실패

`ADDP FORM`처럼 위쪽이 빈칸이고 아래에 문자열이 오는 열은 추론이 실패합니다
(`could not append value ... of type: str`). 엑셀 시트를 **원시 2차원 값**으로
받아 열 타입을 직접 정하세요: 전부 (None|숫자)면 Float64, 하나라도 아니면 Utf8.

### 10.4 Categorical 캐스팅

bdq/pandas 경유로 오면 반복 문자열 컬럼이 Categorical이 됩니다. polars는
Categorical → 숫자 직접 캐스팅을 막습니다
(`cannot cast categorical types to Float64`). **Utf8을 한 번 거쳐야** 합니다.

같은 함수에서 **문자열 → Datetime은 `cast`가 아니라 `str.to_datetime()` 파싱**
이어야 합니다. cast로는 조용히 전부 null이 됩니다.

### 10.5 python-pptx 셀 병합 순서

**값을 먼저 쓰고 병합하면 텍스트를 이어붙입니다**:
```python
cell(0,1).text = "PA1"; cell(0,2).text = "PA1"
cell(0,1).merge(cell(0,3))     → "PA1\nPA1\nPA1"   ← 버그

cell(0,1).merge(cell(0,3))
cell(0,1).text = "PA1"          → "PA1"            ← 올바름
```
**반드시 병합 먼저, 값 나중.**

### 10.6 xlwings 2차원 값 제약

`range.value`에 2차원 리스트를 넘길 때 **모든 행 길이가 같아야** 합니다.
제목 행만 1열이면 `all elements of a 2d list or tuple must be of the same length`
오류가 납니다. 넘기기 전에 직사각형으로 맞추세요.

### 10.7 matplotlib 히트테스트 좌표

점 클릭 판정용 픽셀 좌표를 **그릴 때 캐시하면 안 됩니다.** 캔버스는 그린 뒤
레이아웃에서 크기가 바뀌므로 좌표가 어긋나 클릭이 먹지 않습니다.
**클릭 시점에 변환**하세요.

### 10.8 설정 파일 하위호환

필드가 늘거나 줄면 `TypeError: unexpected keyword argument`로 앱이 죽습니다.
dataclass가 **아는 키만 추려서** 로드하고, 파일이 깨졌으면 기본값으로 복구하세요.

### 10.9 Qt 관련

- 루프 변수가 바깥 위젯 변수를 덮어쓰면 위젯이 GC되어
  `Internal C++ object already deleted`가 납니다
- 다이얼로그 초기화 중 아직 만들지 않은 위젯을 참조하지 않도록 순서 주의
- 리스트 인덱스를 직접 쓰지 말고 헬퍼로 감싸세요 (그룹 0개일 때 IndexError)

### 10.10 성능

- 표 집계를 `(item × lot × wafer)` 반복 필터링으로 하면 item 20개 × wafer 25장에
  500번 스캔합니다. **group_by 한 번**으로 끝내세요
- ADDP를 행마다 파이썬 함수로 계산하면 100만 행 × 12개 = 1,200만 회 호출입니다.
  **수식을 polars 식으로 번역해 벡터 계산**하고, 번역 불가한 것만 폴백하세요
- SQL 미리보기에 item 수만 개를 문자열로 만들면 타이핑마다 멈춥니다.
  `item_id IN ( ... )` + 개수 주석만 표시하고, 실제 목록은 실행할 때만 만드세요
- 같은 alias가 템플릿에 두 번 나오면 polars가 `DuplicateError`를 냅니다.
  집계 전에 중복을 제거하세요

---

## 11. 부가 기능

### 11.1 Excel 캐시

xlwings 호출이 주요 병목입니다.
- 한 파일에서 여러 시트를 읽을 때 **Excel을 한 번만** 띄우기
- 읽은 결과를 parquet로 로컬 캐시 (`%APPDATA%\ETReport\xlcache`)
- 원본의 **수정시각+크기**가 바뀌면 자동 무효화
- 도크에 캐시 현황·비우기 버튼, 리포메터 창에 [새로 읽기](캐시 무시)

### 11.2 SQL 조회 · 내보내기 창

적재된 DuckDB에 자유 SQL(읽기 전용)을 실행해 CSV(BOM 포함) / parquet / SBDF로
저장. 예시 스니펫 제공, 200행 미리보기.

### 11.3 제외 포인트 영속

분석이 읽기 전용이므로 원본 DB에 쓸 수 없습니다.
`%APPDATA%\ETReport\exclusions\<DB이름>_<해시>.json`에 DB 경로별로 저장해
세션 간 유지합니다.

### 11.4 템플릿 예시 다운로드

**상단 [템플릿] 메뉴**에서 리포메터·plot·table·실험조건 예시를 내려받기.
xlsx에 **'설명' 시트**를 함께 넣어 컬럼 의미·규칙·예시를 표로 정리합니다.
Excel이 없거나 저장에 실패하면 CSV + 설명 CSV로 폴백.
[도움말] 메뉴에 네 파일의 관계도.

### 11.5 자동 업데이트

사내 GitHub Enterprise 릴리스를 시작 시 조회해 새 버전이면
**[지금 업데이트 / 나중에 / 이 버전 건너뛰기]** 창을 띄웁니다. 강제하지 않습니다.

**[확정] zip 폴더 교체 방식** — 실행 중 exe는 잠기므로 단일 exe 교체가
불가능합니다. `--onedir` 결과를 zip으로 배포하고, 배치 스크립트가 프로세스
종료를 기다렸다가 `robocopy /MIR`로 폴더를 덮어쓴 뒤 재시작합니다.

확인 실패는 조용히 무시(앱 시작을 막지 않음), 건너뛴 버전은 설정에 기록.

### 11.6 기존 DB 호환

사용자가 손으로 만들어 쓰던 DuckDB를 **읽기 전용**으로 그대로 읽습니다.
- 테이블: `et_data` 우선, 없으면 `fact`, 그것도 없으면 목록에서 선택
- 컬럼 이름은 별칭 사전으로 자동 인식
  (`root_lot_id|lot_id|lot`, `wafer_id|wafer|slot_no`, `chip_x_pos|die_x`,
  `tkout_time|create_dttm|meas_time` …)
- `item_id`/`value` 형태의 long 테이블이면 읽으면서 PIVOT으로 wide화

---

## 12. UI 디자인

애플풍 라이트 테마. 흰 배경 카드, 알약 버튼, 세그먼트 컨트롤, 시스템 블루 액센트.
plot 배경은 반드시 흰색(PPT에 그대로 들어감).

- 상단바: `ET Report` 로고 + [데이터 | 분석] 워크스페이스 전환 + DB 상태 pill
- 변경 후 아직 반영하지 않은 액션 버튼은 **주황색**으로 표시

---

## 13. 폴더 구조 제안

한 파일에 몰아넣지 말고 역할별로 나눠주세요.

```
app.py                    개발 실행 진입점 (python app.py)
src/etreport/
├─ app.py                 부팅: 설정·카탈로그 → 상태 → 창 → 업데이트 확인
├─ paths.py               %APPDATA%\ETReport 하위 저장 위치
├─ demo.py                DB 없이도 화면이 도는 샘플 (--demo 플래그)
├─ config/
│  ├─ settings.py         프리셋 JSON 영속화 (하위호환 주의)
│  └─ catalog.py          bdq.getColumnInfo 캐시
├─ update/                checker.py · dialog.py · apply.py
├─ data/
│  ├─ xlio.py             ★ xlwings 단일 창구 + parquet 캐시
│  ├─ querybuilder.py     타입 인식 조건 → SQL
│  ├─ extractor.py        청크 플래너 · 병렬 · 스키마 정규화
│  ├─ reformatter.py      ★ 리포메터 파싱 · ADDP 수식 엔진(벡터화)
│  ├─ db.py               DuckDB 적재 (et_data)
│  ├─ compat.py           기존 DB 스키마 자동 인식
│  ├─ loader.py           ★ DB → 상태 (step_seq 병합 · ABSOLUTE)
│  └─ exclusions.py       제외 포인트 사이드카
├─ model/
│  ├─ state.py            AppState + StateBus (단일 진실)
│  ├─ specs.py            PlotSpec/PageSpec/TableRowSpec · 자릿수 규칙
│  ├─ templates.py        템플릿 로더 + 검증 (CAT 동적)
│  ├─ split.py            실험 매트릭스 · 혼입 감지
│  ├─ aggregate.py        ★ group_by 집계 (화면·xlsx·PPT 공용)
│  └─ session.py          [적용] 파이프라인
├─ render/
│  ├─ ranges.py           축 규칙 단일 진실
│  ├─ mpl_renderer.py     matplotlib (화면 = PPT)
│  └─ pptgen.py           ★ PPT 조립 (16:9 · 표 전용 페이지)
├─ export/
│  ├─ excel.py            Summary 집계 · xlwings xlsx
│  ├─ deckbuild.py        AppState → PPT 브리지
│  ├─ template_writer.py  UI 배치 → plot 템플릿 되쓰기
│  └─ templates_sample.py 예시 파일 생성
└─ ui/
   ├─ mainwindow.py       워크스페이스 전환 + 상단 메뉴
   ├─ data_ws.py          추출 화면 + 진행 로그
   ├─ analysis_ws.py      도크 + 탐색/Summary/리포트 탭
   ├─ style.qss
   └─ widgets/            자동완성 · 카드 · 캔버스 · 각종 다이얼로그
build/build_release.py    PyInstaller → zip → gh release
```

★ = 함정이 몰려 있는 파일. 특히 주의해서 작성하고 테스트해주세요.

---

## 14. 검증 체크리스트

구현 후 아래를 직접 돌려 확인해주세요. 오프스크린 Qt로 캡처까지 뜨면 확실합니다.

**데이터**
- [ ] `item_id IN` 25,000개 → 3그룹 분할, 각 쿼리가 상한 이하
- [ ] 기간 조건이 문자열 리터럴로 나가는지
- [ ] Categorical + 문자열 tkout_time이 섞인 결과가 정상 파싱되는지
- [ ] ADDP: `ABS`/`LN`(자연로그)/`LOG`(상용로그)/`Std` 행·벡터 경로 값 일치
- [ ] ALIAS 중복 → 마지막 행, 참조 불가 ADDP → 연쇄 제외
- [ ] 중복 재적재 시 0행 증가

**분석**
- [ ] x가 seq 1, y가 seq 2인 데이터로 산점도가 그려지는지 (★)
- [ ] ABSOLUTE가 음수로 적재된 기존 DB에도 적용되는지 (★)
- [ ] 그룹 배정 후 표에 **배정된 lot만** 나오는지
- [ ] [적용] 전에 그룹 편집 [조회]가 동작하는지 (★)
- [ ] 4단 필터를 좁히면 유효 wafer와 포인트 수가 따라 바뀌는지
- [ ] 점 클릭 제외가 탐색·리포트 양쪽에서 되는지, 재시작 후에도 남는지

**출력**
- [ ] PPT 슬라이드 비율이 1.778인지 (★)
- [ ] 순서가 `plot 전부 → 표 전부 → 이력`이고 표가 중복되지 않는지 (★)
- [ ] 표의 lot 헤더가 `PA1` 하나로 나오는지 (`PA1\nPA1` 아님) (★)
- [ ] CAT4·CAT5까지 있는 템플릿이 화면·xlsx·PPT에서 모두 렌더링되는지
- [ ] 템플릿 예시 4종이 저장되고 '설명' 시트가 들어가는지

**성능** (wafer 25장 × item 20 기준)
- [ ] [적용] 1초 이내
- [ ] [표 만들기] 0.1초 이내
- [ ] SQL 미리보기가 타이핑을 막지 않는지

---

## 15. 구현 순서 제안

1. **뼈대** — 폴더 구조, `app.py`, 설정 영속화, 메인 창 전환
2. **데이터 계층** — `xlio`(캐시) → `reformatter` → `querybuilder` → `extractor` → `db`
   여기서 §10.3·10.4·10.6 함정을 먼저 처리
3. **분석 로딩** — `compat` → `loader`(step_seq 병합·ABSOLUTE) → `state`
   §10.1·10.2가 핵심
4. **렌더링** — `ranges` → `mpl_renderer` → 탐색 탭
5. **집계** — `aggregate`(group_by) → Summary 탭 → `excel`
6. **PPT** — `pptgen`(16:9·표 전용 페이지) → 리포트 구성 탭
7. **부가** — 실험 조건, SQL 창, 템플릿 예시, 자동 업데이트

각 단계마다 검증 스크립트를 함께 작성해 돌려주세요. UI는 오프스크린
(`QT_QPA_PLATFORM=offscreen`)으로 캡처해 확인하면 빠릅니다.
