# ET Report — 추가 기능 구현 개발 프롬프트

> 본 문서는 Claude(개발 에이전트)가 ET Report 툴에 **신규 기능 3종**을 구현할 때
> 따를 지침이다. 작업 시작 전 반드시 본 저장소 루트의 `CLAUDE.md`를 읽고
> 프로젝트 컨벤션(한국어 문서·주석·UI, 단일 진실 규칙, 지연 계산 규약 등)을
> 숙지한다.

---

## 0. 사전 컨텍스트 (반드시 먼저 파악할 것)

- **추출 파이프라인**: `data/querybuilder.py`(조건→Impala SQL) → `data/extractor.py`(청크 분할·4워커 병렬·long parquet 고정 스키마) → `data/reformatter.py`(리포메터) → `data/db.py`(`pivot_and_load`, DuckDB `et_data`).
- **분석 파이프라인**: `ui/analysis_ws.py` + `model/session.py`(`apply_config`가 리포메터→템플릿→실험조건→DuckDB를 한 번에 읽고 `LoadReport`로 돌려줌, 예외 미발생).
- **Split/Grouping**: `model/split.py` — `SplitMatrix`, factor 그룹핑, 혼입(confound) 감지가 이미 구현돼 있음. **기능 A는 이를 확장**하고 새로 만들지 않는다.
- **집계 단일 진실**: `model/aggregate.py`(wafer 평균/n-1 표준편차, 화면·xlsx·PPT 공용). **기능 B는 이를 재사용**한다.
- **렌더 단일 진실**: `render/mpl_renderer.py`(화면=PPT). plot은 이 렌더러만 사용.
- **제약**: Linux/WSL에서는 `bigdataquery`(bdq)·`xlwings`가 없어 ImportError로 떨어진다. Excel/xlwings 경로는 코드 리뷰로만 검증. 테스트는 `xlio.read_sheet`를 가짜로 바꾼 `fake_sheet` 픽스처로 돈다.
- **문서·주석·UI 문자열은 모두 한국어**로 작성(신규 코드도 동일).

---

## 1. 기능 A — Fab tracking 기반 자동 Split Grouping

### 1.1 추출 대상
- 테이블: `fab.f_fab_tracking` (기존 `eds.f_et_test`와 **별개** 소스)
- 추출 방식: `df = bdq.getData(sql)` (기존 `data/extractor.py`의 `_fetch` 패턴 재사용)
- 기본 조건: `line_id = 'KFBK'`
- 조회 우선순위: `ein_ecn_no IS NOT NULL` 인 행을 우선 listup (split 실험 대상 식별)
- 가져올 컬럼(원본명 유지):
  `part_id, process_id, step_seq, root_lot_id, wafer_id, area, tkout_time,
  foup_id, eqp_model, eqp_id, unit_id, chamber_id, ppid, reticle_id,
  ein_ecn_no, line_id`

### 1.2 요구사항
1. **단일 Lot 자동 Split → Grouping 자동화**
   - 한 Lot이 진행된 `step_id`들의 `ppid`/`recipe`를 **유효 wafer 내**에서 비교.
   - 서로 다른 `step_id`(=실험 조건이 바뀐 지점)만 listup 하여 실험 정보를
     자동으로 grouping.
   - `recipe` 확인 규칙: `area = 'PHOTO'` 인 경우에만 `recipe`를 보고,
     그 외 area는 `ppid`를 확인.
2. **멀티 Lot 확장**: 위 로직이 여러 Lot에 대해서도 동작하도록 한다.
3. (1.1의 `ein_ecn_no` 조건과 연계하여) split 실험 Lot을 먼저 식별.

### 1.3 구현 가이드 (기존 코드 재사용)
- 추출: `data/querybuilder.py`에 `fab_tracking` 용 빌더를 추가하거나 기존
  `build_extract_sql`을 소스 테이블·컬럼만 다르게 호출하도록 공용화.
  `data/extractor.py`의 청크 플래너·`normalize_schema`(타입/Categorical/Datetime
  함정 처리)를 그대로 재사용 — **컬럼명은 원본 그대로 유지**(`extractor.py`
  점검 결과: 추출 경로에서 축약/변형 금지).
- Grouping: `model/split.py`의 `SplitMatrix`/factor 그룹핑/혼입 감지 로직을
  확장. fab_tracking 정보를 factor 후보로 주입.
- UI 연동: `ui/analysis_ws.py` 도크 또는 `model/session.py`의 `apply_config`
  흐름에 fab_tracking 로딩 단계를 끼워 넣는다(기존처럼 예외는 `LoadReport`로).

### 1.4 인수 기준
- `line_id='KFBK'` + `ein_ecn_no IS NOT NULL` 로 추출 시, 단일 Lot 내에서
  `area='PHOTO'`는 recipe 기준·그 외는 ppid 기준으로 step 간 차이가 나는
  지점이 정확히 listup 된다.
- 추출 컬럼명이 원본(`part_id`, `ppid`, `ein_ecn_no` 등) 그대로 보존된다.
- 멀티 Lot 입력에서도 grouping이 깨지지 않는다.
- `model/split.py` 외부에 별도 grouping 로직을 중복 생성하지 않는다.

---

## 2. 기능 B — Inline 계측 정보 분석 활용

### 2.1 추출 대상
- 테이블: `fab.f_fab_wf_met`
- 추출 방식: `df = bdq.getData(sql)` (기능 A와 동일 extractor 패턴)

### 2.2 요구사항
1. **사용자 입력 → 매칭 → 분석 축 활용**
   - 사용자가 템플릿 또는 엑셀에서 복사한 형식(= `step_id` + `item_id` 목록)을 받는다.
   - 해당 `step_id`/`item_id`의 `fab_value`를, **현재 분석 중인 et data lot**에
     해당하는 것들만 추출하여 매칭.
   - 매칭된 값을 **Summary table**과 **scatter plot의 x축**에 활용.
2. **subitem_id 처리 규칙**
   - **site level**: `subitem_id`에서 `RANGE, STD, MIN, VALUE, SLOTID, Q2, MAX`
     를 제외한 나머지를 사용.
   - **wafer level**: `Q2` 값을 사용.
3. **유의미 인자 top-k 추출 → PPT 리포트**
   - 분석 대상 lot로 **한정**하여 전체 계측 item에 대한 통계 분석.
   - 가장 유의미한 상관관계 또는 group 간 유의차를 만드는 인자를 **top-k** 추출.
   - 추출 결과를 PPT에 리포트(기존 `render/pptgen.py` 연동).
   - item 필터: `item_id REGEXP 'CD|THK|DEPTH|TIP|RCS'`.

### 2.3 구현 가이드
- 집계: `model/aggregate.py`(wafer 집계 공용, group_by 1회) 재사용 — 화면·xlsx·PPT
  가 따로 계산하지 않도록.
- 플롯: `render/mpl_renderer.py` 재사용(화면=PPT 단일 진실).
- PPT: `render/pptgen.py`의 기존 슬라이드 생성 흐름에 top-k 리포트 슬라이드를 추가.
- 매칭: 현재 `AppState.data`(et data lot)와 fab_wf_met를 `root_lot_id`/`wafer_id`/
  `step_id`/`item_id`로 조인. `data/compat.py`의 role 추론을 활용해 컬럼명 차이 흡수.

### 2.4 인수 기준
- 템플릿/엑셀 복사 형식으로 받은 `step_id`/`item_id`에 대해 et data lot과
  정확히 매칭되어 Summary table·scatter x축에 반영된다.
- site level은 `RANGE/STD/MIN/VALUE/SLOTID/Q2/MAX` 제외, wafer level은 `Q2` 적용.
- 분석 대상 lot로 범위가 한정되며, `item_id REGEXP 'CD|THK|DEPTH|TIP|RCS'` 필터가
  통계 분석에 적용된다.
- top-k 결과가 PPT 슬라이드로 출력된다.
- **지연 계산 규약 준수**: 버튼 트리거(또는 기존 [표 만들기]/[그리기]/[미리보기])
  이전에 자동 재계산하지 않는다.

---

## 3. 기능 C — S3 Driver (boto3) 저장/조회

### 3.1 배경
- 사내 S3 호환 스토리지에 `duckdb`, `csv`, `sbdf` 파일을 저장/조회.
- Endpoint: `endpoint_url = "http://s3.daaplatform.samsungds.net:9020"`
- 인증: 개인별 API key 발급 방식(AccessKey/SecretKey).

### 3.2 요구사항
1. **로그인 창 구현**
   - 입력 항목: `Namespace name`, `Bucket name`, `AccessKey ID`,
     `Secret Access Key`
2. **폴더 구조 UI**
   - 입력값 기준으로 해당 namespace/bucket의 폴더 구성을 보여주는 UI/창 구성.
3. **업로드/다운로드**
   - 폴더를 선택하면 해당 폴더로 **업로드**하거나 **다운로드**하는 코드.
   - 저장 포맷: `duckdb`, `csv`, `sbdf` 지원.

### 3.3 구현 가이드
- 신규 모듈: `data/s3.py` 또는 `export/s3.py` (저장소 위치는 기존 `export/`
  와의 정합성 고려 — duckdb/csv는 `data/`, sbdf는 `export/` 계열).
- 클라이언트: `boto3` + 위 `endpoint_url`.
- 인증 정보 영속: `settings.json`(기존 패턴) 또는 `%APPDATA%\ETReport` 하위
  (`paths.py`의 `appdata_dir()` 활용). **Secret은 평문 저장 지양** — 최소한
  OS 자격 증명 분리 또는 파일 권한 제한 권고(구현 전 확인 사항 참조).
- UI: `ui/` 하위에 로그인 다이얼로그 + 폴더 트리 위젯 추가. 모달 창은
  headless 테스트에서 멈추므로 `no_modal_dialogs` 픽스처로 가로채는 경로 마련.
- 기존 "SQL 조회·내보내기" 흐름(`ui` 도크)과 일관되게 연동.

### 3.4 인수 기준
- 4개 입력값(Namespace/Bucket/AccessKey/Secret) 로그인 창이 동작.
- 입력 후 해당 ns/bucket의 폴더 구조가 표시된다.
- 폴더 선택 → duckdb/csv/sbdf 업로드·다운로드가 정상 수행된다.
- 인증 정보가 안전하게 영속/로드된다.

---

## 4. 공통 제약 / 금지 사항

- **단일 진실 준수**: 축 범위(`render/ranges.py`), 자릿수(`model/specs.py:
  fmt_value`), wafer 집계(`model/aggregate.py`), 그리기(`render/mpl_renderer.py`)
  는 우회하지 않는다.
- **타입 안전성**: `as any`, `@ts-ignore`, `@ts-expect-error` 금지(Python이므로
  `type: ignore` 무분별 사용 금지, mypy/ruff 통과).
- **컬럼명 보존**: 추출 경로에서 컬럼명을 축약/변형하지 않는다(점검 완료 사항).
  `die_x`/`site_cnt`/`slot_no` 같은 축약형은 `data/compat.py`의 읽기 호환
  별칭 사전 용도일 뿐, 새 DB 적재 컬럼명을 바꾸지 않는다.
- **지연 계산 규약**: 표/plot/미리보기는 버튼 트리거, 변경 시 주황색 버튼.
- **오류 처리**: 중단하지 않고 건너뛰고 `warnings`/`LoadReport`로 보고(기존 방식).
- **커밋/PR**: 명시적 요청 없이 수행하지 않음.

---

## 5. 구현 전 확인 사항 (개발자가 먼저 물어볼 것)

1. **S3 Secret 영속**: `settings.json` 평문 저장 허용 여부, 아니면 OS 자격 증명
   분리/암호화 필요 여부.
2. **fab_tracking 추출 파이프라인**: 기존 `extractor.py` 청크·워커 구조를 그대로
   공용으로 쓸지, 별도 경량 경로를 만들지.
3. **기능 B 매칭 키**: et data lot과 `fab.f_fab_wf_met` 조인 키가
   `root_lot_id/wafer_id/step_id/item_id` 외에 추가되어야 하는지(예: `chip_x_pos`
   등 die 좌표).
4. **기능 A/B 연계**: fab_tracking 기반 auto-split group이 기능 B의 분석 lot
   범위 한정과 어떻게 연결되는지(공통 Lot 컨텍스트 공유 방식).
5. **UI 진입점**: 각 기능의 진입 UI가 기존 [데이터]/[분석] 워크스페이스 중 어디에
   붙는지(신규 탭/도크 버튼).

---

## 6. 검증 체크리스트

- [ ] `ruff check .` 통과
- [ ] `pytest` 기본(비 slow) 통과 — 신규 로직에 대한 단위/통합 테스트 추가
      (bdq·xlwings 없이도 도는 구조, `fake_sheet`/`factory.py` 패턴 활용)
- [ ] 추출 컬럼명 원본 보존 확인(축약/변형 없음)
- [ ] `model/split.py`(A), `model/aggregate.py`(B), `render/*`(B) 중복 로직 없음
- [ ] 지연 계산 규약 위반 없음
- [ ] S3 업/다운로드 경로가 headless 테스트에서 모달로 멈추지 않음
- [ ] 문서·주석·UI 문자열 한국어
